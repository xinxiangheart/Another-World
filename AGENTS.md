# Another World - 项目约定

## 文档地图（先读这个）

- **规则**全在本文件 `AGENTS.md`：画风基准 / 卡面内容准则 / 前缀配色 / 气质层 / 界面与场景美术方向 / 工程口径。**改规则只改这一份。**
- **卡面（立绘）怎么做** -> `Docs/卡面制作指南.md`（路径 · 命名 · 规格 · 出图 · 抠图 · 落盘 · 验收）
- **卡面做到哪了** -> `Docs/卡面台账.md`（全库总账 · 缺口 · 边带体检 · 待确认项；数字必须实测后再改）
- **发版** -> `Docs/发版流程.md` + `Tools/Release.ps1`
- **联机** -> `Docs/Steam联机最小可复刻方案.md`
- **外部技能（装在 `~/.codex/skills`，不在仓库）** -> `Docs/生图技能清单.md` —— 生图 / 设计类第三方技能台账（装了什么、管什么、怎么卸）
- **外部参考件（在桌面，不在仓库）** -> 见下面「参考件索引」—— `图片参考.docx`（背景 / 视角 / 细节）与 `脸部参考.docx`（脸）
- 引用本文件时**按标题搜，不要按行号** —— 它一直在增补，行号会漂。

## 参考件索引（外部参考件，放在桌面，不在仓库里）

**出卡面前先打开这两份。** 它们不在 git 里，路径固定；**两份都不进模型**（出图线路是纯文本，没有图像输入通道），只能把人读出来的东西**写进提示词**。

| 文件 | 管什么 | 不管什么 | 实测 |
|---|---|---|---|
| `C:/Users/22589/OneDrive/Desktop/图片参考.docx` | **背景 / 细节** —— 环绕式小背景、衬托物的形态与占位、朝向。**取景与视角自 2026-09-26 起不照它**（它是 32/32 全身构图，照抄会把脸压到个位数像素 —— 见「商业化卡面基准（召唤物）」） | 不管脸；**精细度也不取**；**取景也不取** | **32 张 PNG**（明日方舟立绘，658×658 为主） |
| `C:/Users/22589/OneDrive/Desktop/脸部参考.docx` | **脸** —— 二次元脸的画法（**杏仁眼**、细上睑线 + 外眼角短尖、浅上睑褶、一块基色虹膜 + 一枚小高光、小鼻、小嘴、**刘海压住眼上缘**；**无腮红**），是**脸模基准** | 不管构图、不管背景、不管精细度 | **31 张 PNG**（立绘裁下来的**头肩特写**，脸占 200–400px） |

**分工（2026-09-26 定）**
- **画风不取这两份** —— 画风照本文「卡牌立绘风格基准」那串前缀（平涂赛璐璐 / 2–3 级硬边 / 粗线闭合），用户 2026-09-26 明确「画风仍要保持之前要求的」。
- `图片参考.docx` 取 **姿势 / 身体 / 背景** 三条轴（**视角**自 2026-09-26 起降为可选参考 —— 它 32/32 是全身构图，照抄会把脸压到个位数像素）—— 逐轴规则见「半背景（衬托）画法」与「商业化卡面基准（召唤物）」的「取景一改，其余三轴跟着改」。
- `脸部参考.docx` 取 **脸** —— 逐项见「脸模」条（那里有照抄用的提示词片段）。
- **两张表不要混用**：`图片参考.docx` 里也有脸（二次元），但那是**全身立绘**，脸只占画面宽 4–8%，**不足以当脸模**；脸一律看 `脸部参考.docx`。

**怎么打开**（docx 里的图不是独立文件，要看得先解包）

```powershell
python -c "import zipfile,os;z=zipfile.ZipFile(r'C:\Users\22589\OneDrive\Desktop\脸部参考.docx');[open(os.path.join(os.environ['TEMP'],os.path.basename(n)),'wb').write(z.read(n)) for n in z.namelist() if n.startswith('word/media/')]"
```

> **实测（2026-09-26）**：两份都能正常解包，`word/media/` 下**全是 PNG** —— `图片参考.docx` 32 张、`脸部参考.docx` 31 张。解包后按序号看图即可。

---

## 发版流程（Release）

Windows 包发布走 GitHub Release：改 `Assets/_Game/Scenes/Welcome.unity` 的 Inspector `currentVersion`
→ push `main` → `auto-release.yml` 自动打 tag、Unity batch 构建、上传 zip。
一键入口 `Tools/Release.ps1 -Version X.Y.Z`；完整规格、触发链与排错见 `Docs/发版流程.md`。
发版前先确认 self-hosted runner（`C:\actions-runner`）在跑 —— 它不在时任务会一直排队且 GitHub 不报错。

## 界面 / 场景美术方向（2026-09-25 定，出 UI / 场景贴图前必读）

**一句话：界面美术不再走「酒馆风」（暖木 / 米黄实心饼 / 棕圈 / 木拱那套），一律与 `Game.unity` 的 3D 战场同源 —— 深蓝黑石面 + 金细线。**

被换掉的旧信号：`Resources/UI/Phase Wheel.png` 的木拱、阶段圆环的「米黄实心饼 + 棕圈」；2026-09-23 重做顶栏时已改成「深蓝黑凹井 + 金圈」。

**现役样板（这一族互为基准，都已在场景里）**

| 套 | 管什么 |
|---|---|
| `board-layers-v2` | 战场八层（`Board3D_*` 材质 + 环带自转 / 微尘着色器） |
| `cardframe-v7` | 卡面框全组件（卡身 / 金线 / 名牌 / 画窗 / 数值条 / 卡背） |
| `topbar-v1`（生成脚本 `TopBarV2.ps1`） | 顶栏底板、阶段底衬、收起按钮、5 个图标、阶段圆环 |
| `stat-orb-v1` | 左下生命 / 能量 |
| `hud-btn-v1` | 抽牌 / 隐藏手牌圆盘 |
| `icons-v1` | 特性 / 前缀 / 状态图标 |

各套的取舍、尺寸与出图脚本记在 `Assets/_Game/Art/Sprites/Generated/<套名>/README.md`；生成脚本统一在 `Tools/cardframe/`。

**色值（直接抄，别另起一套）**

| 用途 | 色值 |
|---|---|
| 墨（描边 / 最暗） | `#06090E` |
| 卡身底板渐变 | `#121B27 → #090E16` |
| 面板 / 盘面渐变（顶栏、按钮、能量盘共用同一个渐变函数） | `#1E2938 → #0C111A` |
| 棋盘石面 `Board_Plate` 均色 | `#0D141F` |
| 金 | `#C8A44A`（亮 `#E4CB84` / 暗 `#8A6F2E`）；棋盘金取 `Board_Ornament` 最亮金 `#C9AF65` |
| 钢 | `#8EA2B4` |
| 生命 | `#B64848`（亮 `#CE979C` / 暗 `#76282E`） |
| 能量 | `#689AD6`（亮 `#A5C3E7` / 暗 `#3C6498`） |
| 奶油（牌形 / 骨白） | `#D6C298` / `#E0D2B0` / `#9A8664` |

生命 / 能量主色是**取自 `Icon_Health` / `Icon_Energy`** 的，所以顶栏图标、左下圆盘、卡面三处同色 —— 改一处要三处一起改。

**手法：平底 + 一条金细线 + 平板。**

- 要：整块平底、一条金线、相邻色阶只差 **6–8 级**的平板（名牌 / 画窗 / 数值条 / 卡背都这么来的）。
- 不要：**倒角、内阴影、外发光、双层面板、材质贴图** —— 原话是「不做出『凸起』的错觉」（`cardframe-v7`）。
- 母题从棋盘上取：六芒星、菱形铆钉、环带刻度、外圈 16 颗铆钉；卡背的六芒星与棋盘中央同源。

**线宽一律按屏幕像素折算，不许按贴图像素拍**

`hud-btn-v1` 是现成教训：贴图 768px ↔ 屏幕 180px，**1 贴图像素 = 0.234 屏幕像素**，「外墨边 14 / 金环 9 / 发丝线 3」折下来才是 3.3 / 2.1 / 0.7 屏幕像素。出图前先算这个比。

**边界**

- 本条管**界面 / 场景层**：UI 贴图、卡面框、图标、棋盘层、背景图。
- **卡牌原画（立绘）仍按下面「卡牌立绘风格基准」的四张参考卡**，本条不动它（若日后立绘也要并进场景色，单独定一条）。
- **例外**：前缀底图（`prefixbg-v2`）按前缀各一套色相与明度走向，不走这里的单金配色 —— 它管的是「在战场上认出前缀」，别拿它当底色基准。

## 商业化卡面基准（召唤物）—— 2026-09-26 定（出任何一张卡面前必读）

**来由**：用户 2026-09-26 要求「不要被现在任何生成要求限制，客观说明什么才是商业化的卡面」，并问「只露上半身还是全身好」「七分身是不是就是三国杀那种」。本条是那次的实测结论 —— **问题在取景，不在提示词**。

**样本与脚本（可复跑）**

- 我方 **135 张**：`Resources/Cards/Summon/{Hero/1 ×29, Hero/3 ×46, Hero/5 ×30, Special ×23, ChosenOne ×7}`（脚本实测张数；旧文里「136 张」多算了一张，见 `Docs/卡面台账.md` 第 7 节）
- 商业 96 张：Hearthstone / MTG / Runeterra / 游戏王 **各 24**
- 三国杀武将牌 **20 张**；用户参考 `图片参考.docx` **32 张**（明日方舟全身立绘）
- 脚本 `Tools/cardface/refs/`（`fetch_commercial.py` 下载 · `extract_ref_docx.py` 解包 · `face_share.py` 脸占幅 · `baseline_metrics.py` 分家指标 · `thumb_legibility.py` 缩图可读性 · `subject_share.py` 占位；说明见该目录 `README.md`）
- 中间产物与出图对比：`%USERPROFILE%\.codex\visualizations\2026\09\26\01a0db8f-c2df-7563-b487-acff1c7f0cec\`

**先算清楚我们自己的画窗（本节全部结论的地基）**

- 画窗开口（frame 画布 768×1344）= `x76–692 / y268–1043` → 折到卡面 **51×48**、折到原画 **1152×1536 里只看得见中间 925×886**。
- 屏px 有两个口径：预制体 `ArtworkArea` = **64×84**（`Assets/_Game/Prefabs/Cards/Summon/Card00_New_2D.prefab`，`Docs/卡面台账.md` 第 7 节用的就是它）与本节旧记的 **约 90×118**（= 64×84 × 1.4，两处比例自洽，应是别的显示态）。**可见画窗因此是 51×48 或 72×69，都接近正方形**；下面一律按**宽的那个（72×69）**算，让结论偏保守。
- 于是两个毛病同源：**① 按 3:4 画全身 → 顶部 20% 与底部 22% 被卡框盖住；② 全身 7.5 头身塞进 69px 高的画窗 → 头只剩 9px（按 64×84 口径只有 6px），脸更小。**

**四条硬差异（商业 vs 我方，`baseline_metrics.py` / `face_share.py`）**

| 指标 | 商业 96 | 三国杀 20 | 我方 1 费 | 3 费 | 5 费 | 判读 |
|---|---|---|---|---|---|---|
| 主体明度跨度 p10→p90（`refs/baseline_metrics.py`） | **151**（四家 126–204） | — | 71 | **51** | 74 | 商业是全对比，我们是中灰 |
| 缩到卡面尺寸后的明暗标准差 thumbSD（`refs/thumb_legibility.py`） | **48–73**（HS 72.9 · YGO 56.0 · LoR 49.0 · MTG 48.3） | — | 32.1 | **21.0** | 37.6 | 直接量「缩到 90×118 还认不认得出」，我们差 1.3–3.5 倍 |
| 画面「暖色皮肤」占比（`refs/baseline_metrics.py`） | **5.3%**（MTG 17.3） | — | 0.0% | 0.0% | 0.9% | 脸与衣服同灰，缩图后脸先消失 |
| 脸高 / 画幅高（`refs/face_share.py`，口径有坑，只作筛查） | 25%（14.6–40.6%） | **31.8%** | 22.5% | 7.8% | 9.1% | 见下面「脚本口径的坑」 |

> **脚本口径的坑（别当唯一判据）**：`face_share.py` 把「最大肤色连通块」当脸 —— 在全身立绘上会把**脸 + 胸 + 手**并成一块：`图片参考.docx` 那 32 张（脸实际只占画面宽 4–8%）被它量成 23.9%，明显高估。**脸占幅以人眼放大复看为准**，脚本数只做筛查。

**取景（本节核心，硬性）**

1. **人形（Hero / ChosenOne 里的人形）默认「半身偏七分」：头到腰 ~ 头到膝，画窗里可见 3–5 头身。** 全身 7.5 头身**判退** —— 头在 72×69 的画窗里只剩 9 px。
2. **头（含发）在画窗里必须 ≥ 1/5 高**（≈14 屏px，脸 ≈11–12 px）；**目标带 1/4–1/3**（≈17–23 px）＝ 3–4 头身。
3. **「七分身」与三国杀的关系**：三国杀武将牌实测是 **2.5–3 头身的半身~七分**（脸高 31.8%、脸的纵向中心 0.46）；「七分身」（5.5 头身）比它**再远一档**，但同属「不全画」。**我们要站进 3–5 头身**，即「三国杀那一档，最多再远一档」。
4. **`全身` 只留给非人形**：小体型兽 / 器物 / 场域 / 集群 / 坐骑，以及 1 费小个体（「小」本身就是信息）。人形一律不全画。

**取景一改，其余三轴跟着改**

| 轴 | 旧口径（2026-09-25 定） | **2026-09-26 改** |
|---|---|---|
| 姿势 | 站定为主 | 不变；但**手与持物要在画窗内成组**（半身时手在腰线附近，别把手甩出窗） |
| 视角 | **默认俯视**（相机高于胸线 20°–40°） | **俯视降级为可选**：相机只略高于眼线（`slightly high camera angle`），**不许从头顶俯下去** —— 俯角一大脸就被压成一条缝 |
| 背景 | 半背景、四角留空、可环绕两侧 | 不变；人物拉大后背景从「身后一整片」缩成「两侧 + 身后一条」，仍不碰四角与 46px 边带 |
| 衬托物 | 由角色反推、≥3 级明度差、不遮轮廓 | **不变**（四条轴里唯一没动的） |

### 四条设计轴（2026-09-26 定）：服装 / 特效 / 角色细节 / 脸与头发

**来由**：用户 2026-09-26 判「最近生成的图，服装设计、特效设计、角色细节设计、角色面部头发等设计完全不合格」。本节是那次的实测 —— **问题不在「没写清楚」，而在提示词底盘与几条旧禁令把可用的设计词汇全禁掉了**（禁渐变 / 禁辉光、低细节密度、高饱和 ≤0.15）。

**两边分布（`refs/baseline_metrics.py` 口径；`Tools/cardface/design_check.py` 可直接跑）**

| 指标（alpha 内） | 商业 109 张 p25 / p50 / p75 | 三国杀 20 p50 | **我方 135 张 p25 / p50 / p75** |
|---|---|---|---|
| 明度阶梯数（16 桶里 ≥5% 的桶数） | 6 / 8 / 9 | 9 | **3 / 4 / 5** |
| 色相簇数（饱和像素的色相簇） | 4 / 5 / 7 | 5 | **2 / 3 / 5** |
| 细节密度（high-pass 能量） | 10.4 / 13.1 / 17.0 | 20.8 | **5.3 / 7.1 / 10.5** |
| 高饱和占比（S>0.5 且 L>90） | 5.2% / 11.9% / 23.4% | 9.6% | **0.0% / 0.2% / 3.0%** |
| 近白最亮块（L>200） | 3.2% / 8.8% / 25.1% | 10.6% | **0.0% / 0.1% / 2.4%** |
| 近黑占比（L<45） | 8.0% / 16.4% / 35.1% | 26.2% | **7.6% / 21.1% / 59.8%** |

**我方全库 135 张：判退 131 / 仅警告 3 / 过关 1（`01301` 小丑）。** 逐轴不达标率：高饱和 **84%**、最亮块 **77%**、明度阶梯 **76%**、细节密度 **70%**、色相簇 55%、近黑 38%。同口径：商业 109 张 71% 过关、三国杀 20 张 75% 过关 —— **门槛不是刁难，是我们离得太远**。

**门槛（6 项，最多允许 1 项不达标；`python Tools/cardface/design_check.py --all`）**

| # | 项 | 门槛 | 我方中位 | 商业中位 |
|---|---|---|---|---|
| 1 | 明度阶梯数 | **≥ 6** | 4 | 8 |
| 2 | 色相簇数 | **≥ 4** | 3 | 5 |
| 3 | 细节密度 | **≥ 10**（目标 10–20） | 7.1 | 13.1 |
| 4 | 高饱和占比 | **0.04–0.30**（区间） | 0.2% | 11.9% |
| 5 | 近白最亮块（L>200） | **≥ 3%** | 0.1% | 8.8% |
| 6 | 近黑占比（L<45） | **≤ 45%** | 21% | 16% |

**为达标，收回四条禁令（本节的重点）**

| 旧禁令 | 为什么必须收回 | 新写法 |
|---|---|---|
| 「不要喷枪渐变、体积渲染、AO 辉光」 | **特效的本质就是渐变 + 发光。** 我方高饱和中位 0.2% vs 商业 11.9% —— 差 60 倍 | **特效与材质高光放开柔边与发光**：能量 / 光 / 火 / 烟 / 雾 / 火星 / 血滴 / 光带 / 金属与宝石高光，可以渐变、可以发光、可以有软边。**仍禁的只有一件：用软过渡去伪造身体体积** —— 身体与衣物的**底色**仍是 2–3 级硬边平涂 |
| 「低细节密度」 | 我方细节密度中位 7.1 vs 商业 13.1；这三个字写进提示词，等于**主动放弃服装与脸的结构** | 改为**下限 ≥10**。**本文任何地方写的「低细节密度 / 细节密度一律不动」一律作废**，按本条走 |
| 高饱和「≤0.15」 | 这个上限**比商业中位 11.9% 还低**，等于把颜色掐死；旧口径还据此把 `01103` 判成「违规样本」 | 改成**区间 0.04–0.30**；`01103` 那条「柔边发光的能量线」**恢复为可学样本**（当年判它违规的理由作废） |
| 把「近白高光块」当成「为过门硬凑」 | 我方近白中位 0.1% vs 商业 8.8% —— 画面里**根本没有高光** | 近白 ≥3% 是**硬门**：金属反光 / 宝石 / 能量芯 / 眼神光 / 光源窗口，至少一处是真高光 |

**四条轴怎么写进提示词（人形角色照抄进正文）**

- **服装**：≥3 层（外套 / 内衬 / 束带或披挂）+ ≥2 处构件（镶边 / 扣件 / 系带 / 甲片 / 纹样块）+ **≥2 种材质**（布 / 金属 / 皮革 / 宝石…）；相邻两层衣料在**明度或色相上至少差 2 级** —— 这是「明度阶梯 ≥6」的来源。 **褶皱只写 2–3 个大形**（`two or three large folds, no fine crease lines, no crinkle texture, no velvet or leather grain`）—— 用户 2026-09-26 判「不必要的琐碎褶皱过多」；细褶既吃缩图可读性，又引诱模型走写实布褶。
- **特效**：每张卡**必须有一处明确特效**（角色的能力外化），它是**高饱和与近白的主要来源**。特效**不许碰四角与内缩 46px 边带**（前缀识别照旧）。
- **角色细节**：除已有的「进剪影的独有识别物」外，细碎结构（缝线 / 铆钉 / 刻度 / 符文 / 纹样）按第 3 条门槛走密度；不许用「褴褛破洞」当唯一结构（旧条款照旧）。
- **脸与头发**：脸照「脸模」条不变；**头发单列** —— ≥3 组可辨发束 + 一条高光带；**发型要进剪影**（辨识度的一部分）；发色与肤色明度差 ≥3 级；不许一整块平色。

**边界**：本节把「可用的设计词汇」还给画师与模型，**不放松**透明底、边带、画窗、取景（半身偏七分），也不动前文的明度跨度 / p90 门。**前缀细化各节（机械 / 渊 / 灵能 / 神灵画卷）里的「低细节密度」自本条起只读作「不堆砌无意义零件」** —— 它决定**结构类型**，不决定**密度**；密度一律按本节第 3 项（**下限 ≥10**）走。

**出图后必测（本节新增四道，与前文的门并列，不放松任何一条）**

- **取景门**：人形卡放大看第一眼必须是「半身~七分」；**能数出 ≥6 头身即判退**。
- **缩图门**：`thumbSD ≥ 40`（商业 48–73；我方 3 费 21.0 判退）—— 脚本 `Tools/cardface/refs/thumb_legibility.py`。
- **肤色门**：暖色皮肤占比 **≥ 3%**（商业 5.3%，我方 ≈0）。
- **设计轴门**：`python Tools/cardface/design_check.py <图>` —— 上面 6 项**最多 1 项不达标**。
- 三道老门里的「高饱和」按新区间（0.04–0.30）判，其余（明度跨度 / p90）与第 4 道门照旧。

**校准实测（2026-09-26，只 2 张草稿，不是全库结论）**：用新底盘 + 半身取景 + 脸模片段各出 1 张（`--transparent`，ofoxai / `qwen-image-3.0-pro`）。按**同脸尺度**（脸统一 110px、裁成 64:84 画窗，脚本 `Tools/cardface/refs/design_compare.py`）实测：

| | 细节密度 | 高饱和 | 明度跨度 |
|---|---|---|---|
| **新底盘 2 张** | **27.3 / 26.9** | **9.9% / 6.2%** | 104 / 149 |
| 我方现役库 | 7.9 / 10.2 / 12.5 / 13.0 / 17.9 / 19.6 | 0.0–7.2% | 35 / 68 / 74 / 75 / 117 / 186 |
| 商业 4 家 | 9.8 / 11.8 / 16.3 / 34.4 | 3.6–47.5% | 64–215 |
| 三国杀 4 张 | 4.1 / 30.4 / 40.1 / 47.5 | 0.0–42.1% | 152–168 |

**读法**：**细节与饱和度一次到位**（细节 27 已进商业带、远超现役库；饱和从 0.2% 抬到 6–10%）—— 用户判「不合格」的**服装 / 特效 / 细节 / 头发**四轴里，前三轴由底盘直接解决。**明度阶梯与近黑仍未进带**（两张都还是 3–4 级阶梯、近黑仍高）：这一轴不是换底盘能解决的，得靠「相邻两层衣料明度差 ≥2 级」在**具体的服装描述**里落实（实测光写 `no pure black ... keep many separated steps` 无效）。**头发与脸**：两张都出到了「≥3 组发束 + 一条高光带」与可读的二次元脸（脸模片段有效）。

**实测暴露的四条失效模式（出图前必须写进正文，否则会重犯）**：

1. **模型会往画面里塞文字 / UI** —— 第一张出了「`ABYSSAL WEAVER / LVL 99`」铭牌，直接违反「卡面里不许出现『卡牌游戏』」那条。正文只写 `no text` 不够，要写全：`no text, no lettering, no numbers, no labels, no captions, no watermark, no user interface`。
2. **主体会铺到四角** —— 第二张的下两角被斗篷占掉 3%（四角占用门槛是 **0**，见「半背景（衬托）画法」）。正文要加 `a clear flat magenta margin around all four edges`。

3. **画风串被挪到第二段 → 出成照片级写实**（2026-09-26 晚，`01101 复仇者` D 版）—— 第一段先写角色、把风格提示词前缀放到**第二段**的那一张，直接出了**照片级写实**：废墟石柱、写实布褶、没有二次元脸。**模型按第一段定媒介** —— 风格串必须**始终是正文第一句**，重排它等于改规则。
4. **用负向词否定身份 / 风格 → 同样掉进写实区**（同上）—— 为压住「模型画成年轻女法师」而堆的那串 `Not a young woman. No glowing orb in the hand. No staff. No magic circle.` **没起作用**，反而把 `young woman / orb / magic` 当成了关键词，画面掉进写实插画。**负向词只准用于「画面里不许出现的东西」**（`no text, no lettering, no numbers, no labels, no captions, no watermark, no user interface`）；**身份与风格一律正向写**（例：`a lean adult woman in her early thirties, weathered skin, deep-set eyes, a narrow jaw`）。这条与「## 卡面内容准则」不冲突 —— 那里禁的是**内容层**词，这里管的是**别拿否定句描述主体**。

**另记**：人物设定词（性别 / 年龄）**没被遵守** —— 两次都点名要「中年男性、方下颌」，两次都出了年轻女性。所以「脸卡」不能只靠一句描述，骨相与年龄要用更硬的词（`heavy brow, weathered skin, jowls, receding hairline` 这类）单独成句。

---

## 卡牌立绘风格基准（Card Art Style Baseline）

**所有卡牌立绘必须沿用同一套画法基准。2026-09-25 起主参考改为 `01504「群狼之王」` + `01531`（同一配方仅有的两张）；`01103「腐化之心」` 不再担任「唯一画风仲裁」。下面四张参考卡只保留「这类角色该怎么画」的分工，画风仲裁权移交本节末的「辨识度硬指标」与「半背景（衬托）画法」两节。**

**2026-09-26 增补（用户同意）：脸另立一套，脸是「平涂」的例外。** 画风骨架（平涂赛璐璐 / 2–3 级硬边 / 粗线闭合 / 禁渐变）**对脸以外照旧不动**；**脸与眼只放开三样** —— 虹膜边缘一圈略深、一枚小高光（约虹膜 1/10）、一条浅上睑褶线，**几何一律按用户 `脸部参考.docx`（31 张）的二次元画法走**。此前「脸部全硬边」的写法**作废**。**2026-09-26 二次修**：同日第一版曾把「虹膜渐变 / 睫毛 / 泪阜 / 腮红 / 软过渡」全部放开，实测出**浓眼线 + 浓腮红**（用户判「这是什么猎奇脸模」），**现收回**，腮红与浓眼线重新禁。详细规则见本节末的「脸模」条。

基准文件：

- `Assets/_Game/Resources/Cards/Summon/Hero/1/SummonCard_{01103}.png` —— 平涂质感 / 线条的早期样本（**2026-09-25 起退出仲裁**：它是全库渲染最重的一张，且紫色能量线是柔边发光，与本文件自己的「禁渐变 / 禁 AO 辉光」冲突）
- `Assets/_Game/Resources/Cards/Summon/Hero/1/SummonCard_{01112}.png` —— 小体型 / 脆弱感 / 多体同框的处理基准
- `Assets/_Game/Resources/Cards/Summon/Hero/1/SummonCard_{01124}.png` —— 人形 / 优雅 / 主体明确的处理基准
- `Assets/_Game/Resources/Cards/Summon/Hero/5/SummonCard_{01504}.png` —— **库内脸最好的一张**（用户 2026-09-26：「其实群狼之王的卡图算是好的了，其它卡图更差劲」），但**只管全身构图 / 气质 / 明度**；**2026-09-26 起不再作脸模基准**（放大实测低于参考标准，见「脸模」条）

四张各管一段，分工如下：

| 参考卡 | 管什么 | 不管什么 |
|---|---|---|
| 01103 腐化之心 | 平涂赛璐璐质感、2–3 级硬边明度阶梯、线条粗细、细节密度 | 不规定角色的体型、气质与点缀色 |
| 01112 脆弱精灵 | 小体型的画法；同一张卡里多个个体的大小对比与排布；单薄肢体、易碎的观感 | 不管明度结构与线宽 —— 若它的明度 / 笔触与平涂细则冲突，**以「平涂细则」一节为准** |
| 01124 舞者 | 单个人形主体的构图与占幅、优雅姿态 | 同上。**注意：它内部明度跨度只有 25（p90 = 52），是 2026-09-25「灰平」阈值下不达标最严重的一张 —— 只能学构图与剪影，不能学明度分配** |
| 01504 群狼之王 | **库内脸最好的一张** —— 继续当**全身构图 / 气质 / 明度**的库内锚；**2026-09-26 起不再作脸模基准**（放大实测它低于参考标准：虹膜一块平色、被上睑切掉约 40%、无瞳孔无高光、眉几乎贴上睑线、脸一整块平色） | **不管脸怎么画** —— 脸模基准改用用户 `脸部参考.docx`（31 张），见「脸模」条 |

**冲突时以「辨识度硬指标」与「半背景（衬托）画法」两节为准。** 四张参考卡只回答「这类角色 / 这种情况该怎么画」，不定义画风。

### 基准换代（2026-09-25 定）：四张参考卡退出仲裁，改「画法锚 + 硬指标」

**为什么换**：四条理由，都是实测出来的，不是审美分歧。

1. **参考图喂不进模型。** 现行生成线路（`ofoxai` / `qwen/qwen-image-3.0-pro`）是纯文本出图、没有图像输入参数，「喂参考图」这条做不到 —— 本文件在 5 费脸部那节自己就写着「这条做不到，实际做法是把表逐项写进提示词」。既然参考图只能靠人转述成文字，它就不该占着「仲裁」的位置。
2. **01103 自身违反本文件。** 它是全库渲染最重的一张（唯一色数 103069，是 `01136` 的 5.5 倍），紫色能量线是**柔边发光** —— 而本文件在「平涂细则」里明令禁渐变与 AO 辉光。用它仲裁 = 逼全库向一个违规样本收敛。
3. **01504 是离群值，不是中位。** 按它的量值扫全库 136 张，只有它自己和 `01531` 落在同一带里。它不能当尺，只能当**方向锚**。
4. **四张参考卡回答的是「这类角色该怎么画」，从来没有构成一份可执行的 spec。** 这就是 08-28 → 09-17 那二十天里每张单次采样、越走越散的原因。

**因此新的三位一体**（缺一不可）：① **画法锚** = `01504` + `01531`（回答「好是什么样」）—— **2026-09-26 修：`01504` 不再兼脸模基准**，脸模另立，见「脸模」条；② **硬指标** = 下面「辨识度硬指标」那三道门（脚本可判）；③ **提示词模板** = 本文件既有的风格前缀（模型可判）。四张参考卡**降级为构图 / 脸部参考**，不再仲裁画风。

### 辨识度硬指标（2026-09-25 定）：不许再出「灰人」

> **适用范围（2026-09-25 晚修）：本节的三道门、第 4 道门与两条配额只约束 5 费。** 这些门槛是按 `01504` / `01531` / `01502` 那批 5 费样本量出来的；套到 1–3 费上等于「用 5 费手法画 1 费」—— 实测三轮都撞在同一处：要么 p90 / 明度跨度不达标，要么为了达标把 1 费画成 3 费（大面积点缀带 + 近白高光块），档位味全丢。**1–3 费不再据此判退**；它们的画法底线看「1–3 费的脸」那节与下面「平涂细则」的「不动的」一行。

**病灶**：`Hero/1` 29 张 + `Hero/3` 46 张的透明底老稿里，大量是「深灰长袍 / 深灰甲 + 一颗彩色小珠子」，缩到卡面尺寸后互相看不出区别。2026-09-25 抽查 19 张，近中性像素（饱和 < 0.12）占比 > 0.70 的有 13 张（最高 `01348` 0.96），主体内部明度跨度（alpha 内亮度 p10→p90）多数只有 19–42 级；而认可的 `01504` / `01531` / `01502` 分别是 **181 / 164 / 162**（全库中位 71）。

**根因在本文件，不在模型**：「暗黑奇幻基底 + 低饱和 + 全身只准一个高饱和点缀色 + 1–3 费服装不许有设计感」四条叠加，唯一解就是灰袍人。辨识度必须由**至少两条独立通道**承载（形状 + 明度 / 颜色），现在只剩形状一条 —— 而形状差异实测并不小（bbox 归一化剪影 IoU 中位 0.478，只有 18% 的配对 > 0.60），是**缩到卡面尺寸后输给了一团灰**。

**三道硬门（5 费出图后必测，任一不达标即退；1–3 费不适用）**：

| 门 | 门槛 | 拦什么 | 现况（77 张透明底 Hero 卡） |
|---|---|---|---|
| 主体内部明度跨度 | **≥ 80 级**（p10→p90） | 「灰平」 | **49 张（64%）不达标** —— 主病灶 |
| 主体最亮块 | **p90 ≥ 140** | 「整体压暗成一坨黑」 | 7 张不达标（01101 01102 01104 01302 01328 01335 01345） |
| 高饱和像素占比（饱和 > 0.5 且 max > 90） | ~~**≤ 0.15**~~ **2026-09-26 作废 → 区间 0.04–0.30**（见「四条设计轴」） | 「五彩乱来」只适用于**上限**；**下限才是真毛病** | 当年那 8 张「超上限」不再算问题；真正的问题是 **135 张里 114 张低于下限**（中位 0.2%，商业 11.9%） |

三道全过只有 **13 张（17%）**。**门槛是明度纪律，不是加渐变** —— 身体仍是 2–3 级硬边平涂，只是最暗块与最亮块要拉开（例：最暗块 L≈35、最亮块 L≈190）；不许用软过渡去凑跨度。（**2026-09-26**：这三道门是**可读性**门槛，与渲染手法无关；脸是平涂的例外，见「脸模」条。）

**三条松绑（与三道门配套，缺一不可；2026-09-25 晚修后第 2、3 条对 1–3 费只作画法建议）**：

1. **5 费**：「全身只允许一个高饱和点缀色」改为 **一个主点缀色 + 一个次点缀色**，主点缀色**面积下限 4%**（当前多数点缀物只有一颗珠子大小，缩图后看不见）；色相仍按前缀定。**1–3 费不设点缀色的数量与面积上限** —— 按角色定，可多色、可鲜艳（2026-09-25 晚修）。
2. **1–3 费的「服装简单 / 不许有设计感」作废**，与 5 费同规：多层结构（外套 / 内衬 / 束带 / 披挂）+ 至少一处可辨识的剪影特征（肩甲 / 帽檐 / 围裙 / 下摆开衩 / 长柄工具…）。**规模也自 2026-09-25 晚修起不再按费用递减**，按角色定。
3. 「无脸 / 兜帽遮脸 / 纯灰肤」**同一费用档内不超过 1/3** —— 脸是缩图时的第一辨识信号。**这条是画法底线（不是量化「限制」），全档适用。**

**收回一条旧推荐**：`01337` / `01314` 不再作为「半背景先例」推荐。它们的**空间关系**（背景不碰四角）可以学，**对比分配不能学** —— `01337` 内部明度跨度只有 **16**，是全库最平的一张，`01314` 只有 50。

**第 4 道门：两两不许撞脸（2026-09-25 定，针对「一眼看过去一片灰、看不出区别」）—— 只约束 5 费**

上面三道门只管**单张卡自己**，管不住**卡与卡之间**。用户 2026-09-25 指认的那批 17 张已出卡，实测：近中性像素占比 ≥0.88 的有 **11/17**（最高 `01348` 0.97、`01330` 0.94、`01316` 0.93）；主体内部明度跨度达标的只有 **2/17**（`01345` 104、`01135` 87，门槛 80；最差的是 `01316` 24、`01124` 26、`01325` 26、`01323` 28）；主体最亮块 p90 ≥140 的只有 **1/17**（`01348` 213）。所以问题不是「过于同质化」，是**全撞在同一格**。

| 判据（两两之间比） | 阈值 |
|---|---|
| 主体内部明度中位差 | **< 15 级** |
| 主色相角差 | **< 20°** |
| 剪影 IoU（bbox 归一化） | **> 0.55** |

**三条同时成立 = 撞脸，必须改掉其中一张**；两条成立记警告。三道门保单张立得住，这一道保整批分得开。

**配套两条配额（只约束 5 费）**

- **明度分档**：主体平均明度分**暗 / 中 / 亮**三档，任一新图不得让某一档占比超过 **50%** —— 防止整批往同一明度收。
- **独有识别物**：每张卡至少要有一个**进得了剪影**的独有识别物（肩甲 / 帽檐 / 围裙 / 下摆开衩 / 长柄工具 / 坐骑 / 披挂方向…），且不许与该费用档已出的卡重复。

**三项同质化（2026-09-25 逐张读图实测；样本 = 用户指认的 17 张：`01106` `01124` `01125` `01135` `01136` `01309` `01312` `01316` `01319` `01320` `01323` `01325` `01330` `01344` `01345` `01348` `01349`）**

不是「灰」一个毛病，是三条各自独立：

| 轴 | 实测 | 病 |
|---|---|---|
| **脸** | **11/17 没有可读的脸** —— 兜帽阴影吃掉（`01323` `01330` `01344` `01348`）、面罩 / 眼罩（`01316` `01323` `01348`）、黑眼洞（`01106`）、整张空白（`01312` `01319` `01320`）、低头藏掉（`01136`）、只剩半张（`01345`）。有脸的 4 张（`01125` `01309` `01325` `01349`）共用同一套骨相：小眼 + 细鼻 + 无唇 + 下颌平 + 无颧骨 + 无性别年龄特征 | 脸是缩图时的第一辨识信号，却 **65% 不可读**，可读的又是同一张脸 |
| **衣物** | **14/17 是「一件纯色长衣 + 一片布条 / 破洞」** —— 整块斗篷（`01323` `01330`）、纯色高领长衫（`01106`）、交领上衣 + 一块腰布（`01135` `01124`）、破袍（`01136` `01344`）、破背心（`01309` `01312` `01319`）、长袍 + 布包（`01325`）；**靠「褴褛破洞」当唯一结构手段的有 6 张**。真有结构的只有 `01125`（肩甲）、`01345`（背带 + 机械臂）、`01349`（风衣 + 内衬）、`01348`（立领 + 排扣） | 没有层次、没有构件、没有材质区分 —— 读不出「这个人是谁、干什么的」 |
| **颜色** | 每张都是「一个中性色 + 一个小色点」；高饱和占比最高的是 `01135` 的 0.104，多数 <0.03，**没有一张存在明确的色相关系**（互补 / 冷暖 / 同色系三阶） | 不是「颜色少」，是**颜色之间没有关系** —— 整批读成「灰 + 一点」 |

**同质化是成组出现的**（比单张撞脸更伤观感）：兜帽遮蔽组 `01323` `01330` `01316` `01344` `01348`（5 张）、破败组 `01136` `01312` `01309` `01319` `01344`（5 张）、灰衣 + 红布条组 `01135` `01124`。

**由此补三条硬规（与三道门、第 4 道门配套，缺一不可；2026-09-25 晚修后对 1–3 费只作画法建议，不判退）**

1. **脸可读率**：无脸 / 遮脸 / 纯灰肤在同一费用档内 **≤1/3**（原有条款，这里提级为出图后必测）；且**脸型 + 眼型 + 年龄 + 性别的组合在同一费用档内不许重复**。
2. **衣物必须有结构**：至少**三层**（外套 / 内衬 / 束带或披挂）+ **两处装饰构件**（镶边 / 扣件 / 系带 / 纹样块 / 甲片）；**不许以「褴褛破洞」当唯一的结构手段**。
3. **配色必须成关系**：全身色彩必须落进**一个明确关系**（互补 / 冷暖对撞 / 同色系三阶）之一，主点缀色面积 **≥4%**。「中性主色 + 一个孤立色点」不算配色方案。

**1–3 费的脸：结构不许少（2026-09-25 立，2026-09-26 并入「脸模」条）**

旧口径「1–3 费按 `no detailed facial features` 克制处理」**作废**。它对纯文本出图的实际效果是：模型给不出结构，就退回训练分布的中心 —— **同一张脸贴在 17 个身体上**（2026-09-25 实测：17 张里 11 张脸不可读，可读的 4 张共用同一套骨相）。

**2026-09-26 修正**：连渲染也不克制 —— 脸是平涂的例外，虹膜渐变 / 睫毛 / 泪阜 / 腮红 / 软过渡**全部允许**。以下面的「脸模」条为准。

**脸模（2026-09-26 重写）：脸是「平涂」的唯一例外 —— 身体与场景照旧平涂，脸（尤其眼）按二次元画法走。**

**为什么要改**：用户 2026-09-26 判定「脸画的非常难看」，并给了 `脸部参考.docx`（实测 **31 张 PNG**，每张都是立绘裁下来的**头肩特写**，脸占 200–400px）。逐张放大比对后确认：问题不在「没写清楚怎么画脸」（那一版写得比参考还细），而在**规则自己打架** —— 一边要「二次元脸」，一边禁掉了二次元脸的全部成形手段（禁渐变 / 禁软过渡 / 禁腮红 / 脸部全硬边）。**一只二次元眼至少要四处软/渐变元素才成立：虹膜渐变、瞳孔、高光、睫毛**；脸的体积至少要**腮红 + 刘海投在额头的阴影带 + 颈侧阴影**。用硬边平涂块画二次元眼，出来必然就是「一条青缝 + 一块平色」。

> **2026-09-26 二次修（同一天）**：上面这一版**矫枉过正**。按它出的 `01101` 三张，脸被用户判为「**这是什么猎奇脸模**」。病灶是**把参考图的细节读成了硬指令**：`thick black lash line + hooks up and out + 2–3 lash spikes` ⇒ 浓眼线；`soft blush` ⇒ 浓腮红；`one big bright pure white highlight` ⇒ 洋娃娃眼。**参考图（`image20` `image6` `image22` 放大实测）其实是：细到中等的眼线 + 外眼角一个短尖 + 浅褶线 + 小块基色虹膜 + 一枚小高光 + 平脸颊 + 刘海压住眼上缘。** 本条下面的提示词片段与逐条表已按此重写；**腮红回到禁**。

> **画风骨架不动**（用户 2026-09-26 原话：「虽然让你参考明日方舟的画法但是画风仍要保持之前要求的」）：身体、衣物、场景照旧**平涂赛璐璐 + 2–3 级硬边 + 粗线闭合 + 禁渐变**。**只有脸和眼是例外。**

**脸模基准（2026-09-26 改）**：一律照用户 `C:/Users/22589/OneDrive/Desktop/脸部参考.docx`（**31 张**）的二次元画法。

**`01504 群狼之王` 降级说明**：它仍是**库内脸最好的一张**（用户 2026-09-26：「其实群狼之王的卡图算是好的了，其它卡图更差劲」），继续当**全身构图 / 气质 / 明度**的库内锚；但它**低于参考标准，不再作脸模基准** —— 放大实测：**虹膜是一块平色、被上睑切掉约 40%、无瞳孔无高光、眉几乎贴上睑线、脸是一整块平色**。旧口径还给它贴过「全硬边、无腮红、无软过渡」的标签，**那是错的**（它有渐变也有软过渡），说明写那条时没放大看过。

**脸模提示词片段（人形角色每张照抄进提示词正文；纯文本出图没有参考图通道，不写就等于没有这条规则）**：

```
anime face, flat cel shading - the body, the clothes and the background stay hard-edged, and the face and the eyes are the only place where a little softness is allowed, never airbrush rendering:
almond-shaped anime eyes, wider than they are tall;
the upper lid overlaps only a small part of the top of the iris, so the iris still reads as a wide open almond;
upper lash line drawn as one dark line of even medium weight that follows the top edge of the eye and closes into a short thin point at the outer corner - no thick wedge, no cat-eye wing, no eyeliner look, no separate lash spikes, no lash marks under the eye;
upper lid crease drawn as one thin faint line above the lash line, thinner and lighter than the lash line itself - not a second black stroke;
iris as one flat base colour, with a slightly darker ring along its edge and a slightly lighter middle, plus one small soft highlight about one tenth of the iris placed high on the iris - no dark pupil dot, no big white glossy blob, no doll eye;
a thin eyebrow, partly hidden behind the bangs, with a clear band of bare skin between the brow and the lash line;
lower lid drawn as one thin faint line only, no shadow lump under the eye;
front bangs hang low over the forehead and cover the top edge of the eyes and part of the brows, casting one soft shadow band across the forehead;
cheeks are bare flat skin, no blush, no pink cheeks, no cheekbone shading;
small nose drawn as one dot, small mouth drawn as one short line, no lower lip;
skin is warm pale and clearly lighter and warmer than the clothing;
2 to 3 flat tones on the face, not photorealistic proportions
```

**出图后逐条对（2026-09-26 定；下表是从 31 张参考逐张放大读出来的，**必须一条一条数**）**

| 构件 | 参考的画法 | 库内实测（2026-09-26） |
|---|---|---|
| **上睑线** | **细到中等**的深色线，贴住眼上缘，**外眼角收成一个短而细的尖** —— 不是粗楔形、**不外飞成猫眼**、无独立睫毛尖 | **第一版写错**：`thick black + hooks up and out + 2–3 lash spikes` ⇒ 实测出**浓眼线**（`01101` v1–v3），用户 2026-09-26 判「猎奇脸模」 |
| **上睑褶** | 一条**细而浅**的线（`image20` 看得见，`image6` 几乎看不见） | **第一版写错**：`soft crease line ... clearly above` ⇒ 实测被画成**第二条黑线**，与眼线叠成「双眼线」 |
| **虹膜** | 一块**基色** + 边缘一圈**略深** + 中间**略亮** + **一枚小高光（约虹膜 1/10，偏上）** | **第一版写错**：`four layers + dark pupil + one big bright pure white highlight` ⇒ 实测 **大白眼珠**，读成洋娃娃 |
| **上睑压住多少** | 只压住虹膜上缘**一小段**，虹膜仍读成杏仁 | 这条**有效，保留**（第一版唯一写对的一条） |
| **眉** | 细，**大部分被刘海挡住**，与上睑线之间留出皮肤 | 第一版只写「留间隙」、漏了「被刘海挡住」⇒ 实测眉又长又清楚，与眼线叠成三条线 |
| **下睑 / 泪阜** | 下睑**一条细浅线**；泪阜可有可无 | 第一版写「细线 + 下方一条软阴影」⇒ 实测多出一条眼袋状暗块 |
| **刘海** | **压住额头与眼的上缘** —— 二次元脸的必要条件 | 第一版**完全没写** ⇒ 实测刘海停在眼睛上方，脸「平」且眼被放大 |
| **腮红** | 参考**基本没有腮红**（`image20` `image6` 脸颊都是平肤色） | **第一版写错**：`soft blush on the cheeks` ⇒ 实测少年脸上出现**浓腮红**，与眼线一起构成「猎奇」。**回到禁** |

**六条硬约束（2026-09-26 立，同日二次修；缺一条就退回「一条缝」或「猎奇脸」）**

1. **上睑只压虹膜上缘一小段**：`the upper lid overlaps only a small part of the top of the iris`。不写这条，模型默认压掉近一半。
2. **眼线不许外飞、不许睫毛尖**：`no thick wedge, no cat-eye wing, no eyeliner look, no separate lash spikes`。第一版写 `hooks up and out + two or three lash spikes`，实测直接出浓眼线 —— 「猎奇脸」第一来源。
3. **上睑褶不许画成第二条黑线**：`one thin faint line, thinner and lighter than the lash line`。第一版写 `soft crease line ... clearly above`，实测被画成实心黑线。
4. **不许腮红**：`cheeks are bare flat skin, no blush, no pink cheeks`。31 张参考里脸颊基本都是平肤色；第一版写 `soft blush`，实测出浓腮红。
5. **刘海必须压住额头与眼上缘**：`front bangs hang low over the forehead and cover the top edge of the eyes`。第一版没写，实测刘海停在眼睛上方，眼被放大、脸变平。
6. **眉与上睑线之间必须有皮肤间隙**：`a thin eyebrow ... with a clear band of bare skin between the brow and the lash line`。
3. **肤色不受低饱和约束**：肤色是**角色属性**，不参与「低饱和 / 全身只一个高饱和点缀色」的分配，也**不算**那个点缀色。提示词写 `warm pale skin, clearly lighter and warmer than the clothing`。实测依据 —— 画面里「暖色皮肤」占比：库内 **1 费 0.4% / 3 费 0.1% / 5 费 3.2%**，参考 **9.5%**。我们的卡上几乎不存在肤色，脸和衣服同灰，缩图后脸先消失。

**1–3 费的脸**：与 5 费**同规**（2026-09-25 晚修已废掉「1–3 费按 `no detailed facial features` 克制处理」）。**克制的是画面规模，不是脸的结构** —— 上面那七件一件不许少。

**仍禁（这些管的是身体的渲染，不是脸）**：渐变、体积渲染、写实解剖、交叉排线、喷枪、草稿笔触 —— 一律只约束**脸以外**。**脸也不是全放开（2026-09-26 二次修）**：原口径「虹膜渐变 / 睫毛 / 泪阜 / 腮红 / 软过渡全部允许」**收紧**成三样 —— **虹膜边缘一圈略深 + 一枚小高光（约虹膜 1/10）+ 一条浅褶线**。那一版全放开实测出的就是浓眼线 + 浓腮红，即用户判的「猎奇脸模」。

**防「AI 批量脸」四条（出图前先写「脸卡」，出图后逐条对）**

1. **骨相必须有差异**：脸型（方 / 尖 / 圆 / 长）、下颌角有无、颧骨高低、眼距、眼型走向 —— 每张至少在这五项里 **3 项**与同费用档已出的卡不同。
2. **要有年龄与性别**：不许全是「看不出年龄性别的中性青年」；老年 / 中年 / 少年 / 女性要真实出现在各费用档里。
3. **每张一条专属标记**：疤 / 胡须 / 义眼 / 独眼 / 面纹 / 耳饰 / 面具 / 白发 —— 一条就够，但**要进得了剪影**。
4. **组合不许重复**：把「脸型 + 眼型 + 年龄 + 性别 + 专属标记」当五元组，**同一费用档内不许出现同一组合**。

> **骨相差异不要用「小眼 / 窄眼 / 细长眼」实现（2026-09-26 实测）**：这类词会直接打掉脸模的「大眼」，模型折中成「带眼白的写实窄眼」——实测加了 `narrow set narrow eyes` 的那条候选，眼退化成一条缝。差异要用**脸型（方 / 尖 / 圆 / 长）、下颌角、颧骨、发际线、发型、专属标记**去承载，**眼睛一律保持二次元的大眼**。

**判据一句话**：把这张脸单独裁出来给一个不认识游戏的人看，他应该说得出「这是个什么人」（年龄、性格、干什么的）；说不出，就是 AI 批量脸，退。

**不动的**：平涂赛璐璐、2–3 级硬边明度阶梯（**底色**）、线宽统一闭合、透明底。~~低细节密度~~、~~禁渐变 / 体积渲染~~ **这两条 2026-09-26 作废** —— 细节密度改成**下限 ≥10**，渐变 / 发光**对特效与材质高光放开**（只禁「用软过渡伪造身体体积」），见「四条设计轴」。**2026-09-26 例外：脸与眼** —— 见「脸模」条（**只放开**：虹膜边缘略深 + 一枚小高光 + 一条浅褶线；**腮红与浓眼线不放开**）。

### 半背景（衬托）画法（2026-09-25 定）：透明底对全部费用档生效

**「5 费允许满幅背景」（2026-09-14）作废。** 理由不是审美：满幅会盖死 `Frame_ArtWindow` 的四角与边带，带前缀的 5 费卡在战场上**丢失前缀信息** —— 这条实测已记在 `Assets/_Game/Art/Sprites/Generated/prefixbg-v2/README.md` 的「已知边界」。

| 项 | 规则 |
|---|---|
| 四角 + 内缩 46px 边带 | **必须保持透明**（硬性，功能要求 = 前缀识别） |
| 衬托物 | 只允许出现在**画窗中央**：召唤 `x76–692 / y268–1043`，法术 `y268–856` |
| 衬托物画法 | 地形 / 陈设 / 光区。**形体与占位照参考图**（大形 / 环绕 / 贴地 / 不占四角），**明度块仍压到 2–3 个**，不画成环境插画（不取精细度）；与主体保持 **≥ 3 级明度差** |
| 抠图 | 全档同规：加 `--transparent` 出洋红底再抠图（见「抠图注意事项」） |

**取法的样板**：`01504` / `01531` 的**处理**（主体全对比、环境做衬底、近乎单色系、硬边平涂），**不取它们的形态**（满幅）。两者都要，就得用「衬托物限画窗中央」这一条去调和。

> **方向参考**：2026-09-25 表述「非满篇幅，半背景 + 视角参考，不参考精细度，主要参考画法」；同日补充「**不是说精细度，而是姿势，背景，身体，视角等参考**」。即：参考图取 **姿势 / 身体 / 视角 / 背景** 四条轴（下节逐轴展开），**唯一不取的是精细度** —— 精细度由上面三道门约束。

**画什么由角色反推，不由本文件派（2026-09-25 定，最容易用错的一条）**

参考图里出现兽影、废墟、摊位、书房、晶簇，**不是因为它们好用，而是因为那个角色需要一个东西来衬托自己的形象与特点** —— 兽影是那头角色的特点，废墟是那个角色的特点。衬托物是**角色特点的外化**，不是可挑选的装饰模板。

- 顺序永远是：**先读这个角色是谁 → 再决定身后立什么**。反过来做（先挑一件衬托物再套角色）即跑偏。
- **判据一句话**：把这张衬托物换到另一张卡上也成立，就说明它没在衬托这个角色 —— 不合格。
- 因此**本节不提供「衬托物内容清单」**；下表的「大 / 小 / 兽影」只是**体量与形态**的参考，回答的是「占多大、什么形态」，不回答「画什么」。
- 反面先例：`prefixbg` 那种按前缀套母题的做法**不能**平移到衬托物 —— 衬托物按角色定，不按前缀定（前缀只管色相）。

**体量分档（据 32 张参考图实测）**

| 体量 | 用在哪 | 形态 | 占位（实测） |
|---|---|---|---|
| 大（场景层） | **5 费** | 一整片环境，回答「在哪、在做什么」 —— 铺开到画窗大部分、**边缘化开**、四角透出 | 参考占幅 0.94–0.99 × 0.85–0.99、填充 0.60–0.81 |
| 小（道具层） | **1–3 费** | 一件大形物件或一小堆道具 + 一块地台 / 台面，只占画窗下半或一侧 | 四周大量留白；参考填充 0.33–0.55 |
| 单件大形（兽影 / 残骸 / 晶簇 / 触手这类） | **1–3 费也成立**，体量按角色定 | 不铺环境，身后只立**一件**大形 | 占画窗 1/3–2/3、身后且不遮轮廓 |
| 无（兜底） | 任意档 | 纯色带飞白笔触环绕 —— 确实没有东西可立时才用 | 环绕主体，不占画面 |

> 表里第 3 行只是**体量**选项，不是内容选项：`img01/06/11/12` 之所以画兽影、`img14/16/24/30` 之所以画机械残骸与触手，都是那些角色自己的特点，换张卡就不成立。

**参考图给的四条轴（2026-09-25 明确）：姿势 / 身体 / 视角 / 背景 —— 精细度不在其中**

> 用户 2026-09-25 原话：「**不是说精细度，而是姿势，背景，身体，视角等参考**」。本节据此重写：先前只把「不取精细度」当成结论，读起来等于把整批参考图作废；实际上参考图是**画面调度**的基准，精细度由上面三道门单独约束。

**逐轴规则（据 32 张参考图逐张读图 + 脚本实测，原件 `图片参考.docx`）**

| 轴 | 参考图实测 | 抄什么 |
|---|---|---|
| **姿势** | 站定 **19/32**、坐 **5/32**、跑 / 蹲 / 跃 / 挥 **8/32** | **站定是主调**，不是「越动越好」。站定也要有戏：重心压一腿、一脚前一脚后、3/4 侧身、手持物有持法（不是垂手站着）。坐 / 跪 / 蹲按角色可用 |
| **身体** | **32/32 是全身构图** —— 没有一张取胸像 / 半身特写；多数能读到腿脚，少数几张（`img01` `img06` `img09` `img11` `img12` `img16`）**腿脚被身后的暗色大形吃掉** | ~~**全身，不裁头不裁脚**~~ **2026-09-26 作废 —— 这条不抄**：我们的可见画窗只有 **925×886 原画px ≈ 72×69 屏px**，全身构图会让头只剩 9 px。改为**半身偏七分（头到腰 ~ 头到膝，3–5 头身）**，见「商业化卡面基准（召唤物）」。承托物（脚踩地台 / 机械残骸 / 台阶 / 木箱 / 冰台）与「身体是全图唯一的高对比 + 高细节区」「面部与上半身优先」照旧 |
| **视角** | **俯视为主** —— 相机高于人物（从上方看下去，能看到头顶 / 肩线 / 脚下地面）。**2026-09-26 更正**：旧口径写「以平视为主、强俯视几乎不见」，**是错的**；用户 2026-09-26 明确「一般是俯视角」，重看 `rf02` `rf10` 等确认是**明确俯视**。朝向含正面、3/4、侧面、背面 | **俯视**（相机高于人物胸线，常用 20°–40° 俯角）；朝向**不限于面对镜头** —— 3/4、侧、背都在参考里，按角色选。仰视基本不用 |
| **背景** | 填充率中位 **0.48**（≈ 一半画布是空的）；四角基本干净（**20/32 严格为 0**，仅 4 张 > 3%，最高 `img17` 10.3%）；整体横向铺开中位 **0.92**、纵向 **0.92** | **半背景、不满幅**；四角留空（与「46px 边带」同向）；背景**可环绕到主体两侧**，不只是「身后一件大形」 |

**仍守的**（与参考图不冲突，照旧）

- 人物**居中轴**（参考实测 cx 中位 **0.50**），脚**不碰画布下缘**。
- **背景不遮人物轮廓**：参考图允许背景与身体交叠，但面部与上半身始终是高对比区；本卡面缩到卡面尺寸只有约 90×118、**可见画窗只有 ≈72×69**（原画 1152×1536 里只看得见中间 925×886），外轮廓读不出来就废。这是本项目的取舍，不是参考图没有。
- 衬托物降对比、降细节，与主体保持 **≥ 3 级明度差**。
- ~~**俯视要写进提示词**~~ **2026-09-26 改**：俯视不再是默认。要俯只写**轻度**俯视（`slightly high camera angle, the viewer looks down at the character`），**不许从头顶俯下去** —— 半身取景 + 大俯角会把脸压成一条缝。平视合法；仰视仍不用。见「商业化卡面基准（召唤物）」。

**唯一不取的是精细度**：参考图的笔触密度与渲染不抄，衬托物照旧硬边平涂、低细节密度（「平涂细则」不动）。但**「背景画成大形、有结构、有母题」要抄** —— 别把「不取精细度」误读成「背景只能是一块色斑」。

> **参考图出处**：2026-09-25 用户提供的 32 张（明日方舟干员立绘风格），原件 `C:/Users/22589/OneDrive/Desktop/图片参考.docx`（实测 **32 张 PNG**）。取的是 **姿势 / 身体 / 视角 / 背景** 四条轴；**唯一不取精细度**。它们的**脸也是二次元**（用户原话：「我在链接对话里发的那些新参考也大多是二次元的脸」）。**2026-09-26 起脸模不看这一批** —— 这 32 张是**全身立绘**，脸只占画面宽 4–8%；脸模基准改用用户 `脸部参考.docx`（31 张**头肩特写**），见「脸模」条。


已按此基准重绘（2026-09-10，原始内容要求未变）：01101「复仇者」、01102「堕落者」；01103「腐化之心」为首张基准稿；01104「佣兵」、01105「牌场老手」、01107「妖精」由手绘稿改为正式插画；01106「无名之辈」由灰色剪影占位稿改为正式插画（2026-09-11）；01108「搜查官」由抽象占位稿改为正式插画（2026-09-11）。01109「尖啸者」由旧占位稿（恶搞图，未作参考）改为正式插画（2026-09-11）。01117「苦难给予者」为新增卡图（2026-09-11，此前该卡无任何立绘）。01124「舞者」为新增卡图（2026-09-11，此前该卡无卡图），并自即日起与 01103、01112 一同列为风格参考卡。

2026-09-12 批次（01110 碎片、01111 深潜者、01113 传送阵、01114 暴徒、01118 猎犬、01119 认同指令、01125 投机者、01126 凝聚体、01127 改造者、01128 超数故障、01129 滋养者、01136 难民、01302 疗、01303 潜行者、01308 麻烦制造者、01322 残篇、01328 破防者、01523 水墨、01524 画卷之核）同样按此基准出图，2026-09-13 续出 01332 异面信徒（全库唯一双前缀卡）、01333 幻身、01334 集群意识、01335 能量骇客、01336 修正者、01307 阳（新增卡图）、01306 阴（由旧占位稿改为正式插画，与阳互为镜像勾玉）、01301 小丑（由恶搞占位稿改为正式插画）、01305 幼生之龙（新增卡图，首个俯视视角用例）、01317 贪欲之蛇（由恶搞占位稿改为正式插画）、01323 法官（由黑白草图稿改为正式插画）、01310 蜂王（新增卡图，`前缀: 无` 3 费，整体蜜黄基调的非人形蜂后）、01327 阴影聚合体（由恶搞占位稿改为正式插画）、01331 囚牢（新增卡图）、01318 弱化棱晶（新增卡图，`前缀: 无` 3 费，悬浮六棱晶柱：入射青带满饱和、出射压成暗灰青，只改光带色、晶柱未重绘）、01324 猎杀者（新增卡图，`前缀: 无` 3 费，低檐皮帽 + 兽皮短披 / 长柄带钩猎矛 / 脚边巨兽头骨，点缀暗血红）、01326 陷阱（新增卡图，`前缀: 无` 3 费非人形，半埋的铡口巨夹 + 尖桩倒刺绳，点缀警示黄）、01338 深海恶物（由 2000×2666 旧规格稿改为正式插画，`前缀: 无` 3 费非人形，鮟鱇式底栖巨口掠食兽 + 倒钩鳍扣住海床 + 身旁弃壳，点缀海绿荧光）；逐张的形象、取舍与抠图参数记在 `Assets/_Game/Art/Old/README.md` 的「替换记录」表。

被替换下来的旧卡图归档在 `Assets/_Game/Art/Old/`，索引见该目录 `README.md`。

风格特征：

- 杀戮尖塔 2（Slay the Spire 2）的**平涂语言** —— 只约束**底色**（色块硬边、线条统一、剪影优先）；**不约束特效、材质高光与细节密度**（2026-09-26）
- 干净圆润的手绘线条（clean rounded line art）
- 暗黑奇幻基底（`dark fantasy` 是**画风基底**，不等于「每张都必须黑暗」——气质按角色走，见下面「气质层」）。~~低饱和度~~ **2026-09-26 作废** —— 高饱和占比改按区间 **0.04–0.30** 判（见「四条设计轴」）；肤色与脸不受本条约束（见「脸模」条）
- 精致，且**允许繁复**：细节密度现在是**下限（≥10）**，「画得繁复 / 更精致」不再是跑偏信号（见「四条设计轴」）
- 手绘卡牌插画质感

**平涂细则（最容易跑偏，务必守住）**

要：

- 平涂赛璐璐（flat cel）：**底色**明度阶梯只留 2–3 级，相邻色块**硬边**相接（**特效与材质高光不受此限**，2026-09-26）
- 线条粗细统一、闭合、干净
- 剪影优先：缩到卡面尺寸时外轮廓仍要能读出来
- ~~低细节密度：内部只保留必要的结构线~~ **2026-09-26 作废 → 细节密度下限 ≥10**（`design_check.py` 第 3 项，见「四条设计轴」）
- 全身只允许**一个**高饱和点缀色，其余全部压在低饱和灰黑；**这个点缀色的色相由卡牌前缀决定**，见下面「前缀配色」。**该「只准一个」不是铁律**——部分 3 费卡可以无视、5 费卡更不必守，见「前缀配色」里的「多色松绑」条。**2026-09-25 晚修：1–3 费不再受本条与「低饱和」限制** —— 点缀色的数量、饱和度与面积按角色定，与 5 费同规。**2026-09-26 补：肤色不算这个点缀色，也不跟它一起压灰**（见「脸模」条）。

不要：

- 不要**用喷枪渐变 / 软过渡去伪造身体体积与衣物褶皱**（**特效与材质高光不受此禁** —— 2026-09-26 改，见「四条设计轴」）
- 不要交叉排线、草稿式笔触
- 不要解剖写实（肋骨、肌肉束、器官纹理这类细节要抽象掉）
- ~~不要为了「更精致」堆细节~~ **2026-09-26 作废**：细节密度现在是**下限**，不是上限；「更精致」不再等于跑偏（见「四条设计轴」）

验收：把新图和**基准卡**并排放在深色底上、缩到卡面尺寸看。**平涂质感、明度阶梯（底色）、线条**一律对 `01103`；画人形时另对 `01124`（构图 / 占幅 / 剪影），画小体型或多体时另对 `01112`（个体大小对比 / 排布 / 脆弱感）。~~若新图显得「更精致 / 更立体 / 更亮」，即已跑偏~~ **2026-09-26 改**：**「更精致 / 更亮」不再是跑偏信号** —— 只要**底色**仍是硬边平涂、取景（半身偏七分）与边线带守住，细节与高光越多越好。跑偏的真信号只有两个：**底色被软过渡糊掉**、**写实解剖（肌肉束 / 器官纹理）**。

风格提示词前缀（每次生成必须带上，后续描述追加在其后，结尾统一补 `3:4 portrait, no text, no border`）：

```
refined hand-drawn anime-style card game illustration, clean rounded line art, dark fantasy, half-body composition, flat cel shading with hard-edged base colour blocks, bold uniform outlines, rich saturated accent colours, glowing magic and energy effects with soft light falloff, polished metal and gem highlights, layered costume with trims, fasteners and belts, distinct material textures, expressive anime face, grouped hair strands with a highlight band, high detail density, no photorealism, no sketch texture
```

> **为什么换掉旧串（2026-09-26）**：旧串 `low saturation, no gradients, simple clean shapes, low detail density` 四条，正好把用户判「完全不合格」的**服装 / 特效 / 细节 / 脸与头发**四轴全部禁掉 —— 全库实测高饱和中位 0.2%（商业 11.9%）、细节密度 7.1（商业 13.1）就是这串的直接产物。新串保留 `flat cel shading … hard-edged base colour blocks`（底色硬边不动），其余改为**正向要**：`rich saturated accent colours`、`glowing magic and energy effects`、`polished metal and gem highlights`、`layered costume …`、`high detail density`。
>
> **旧的「低饱和底盘」为什么连 5 费都救不回来**：旧串的 5 费变体只换 `low saturation` 一处，实测仍然灰（`01507` 生命祭司平均饱和仅 11.1）。**负向词与「低细节」底盘会压过单个变体** —— 所以现在改的是底盘本身。
>
> **若实测仍偏灰平**：把 `flat cel shading with hard-edged base colour blocks` 挪到最前、把 `rich saturated accent colours, high detail density` 放到**结尾**（结尾注意力最强）；脸模片段照旧放最末。

> **5 费变体（2026-09-13 定，2026-09-26 重锚）**：把 `rich saturated accent colours` 换成 `rich saturated full palette`（饱和色**面积**也放开，不只是点缀），其余一字不动。**1–3 费不再需要这个变体** —— 新底盘里已经没有 `low saturation`。

### 卡面内容准则（2026-09-15 定）：卡面里不许出现「卡牌游戏」

**画面里不允许直接出现「卡牌游戏」这一层东西。** 卡牌的效果（摸牌、减费、护盾、召唤……）必须**翻译成世界观里合理存在的事物**再画出来，不能把界面元素照搬进插画。本条对**所有费用档**成立。

| 不许直接画 | 该画成什么（按角色与世界定，下表只是示例） |
|---|---|
| 扑克牌 / 卡背 / 牌堆 / 手牌 | 卷轴、信笺、货单、账本、票据、碑文 —— 要看得出是这个世界的纸 |
| 抽牌 / 摸牌 / 弃牌 | 把信笺递出去、从箱子里翻出一张单据、撕下一页账目 |
| 费用 / 能量 / 法力水晶 / 费用数字 | 价牌、铜秤、成堆的金币、被划掉重写的小木牌 |
| 卡槽 / 场位 / 格子 / 棋盘 | 真实的地面陈设，靠空间关系暗示站位 |
| 血量 / 攻击力的数字或图标 | 完全不画，靠体态、伤痕、装备读出来 |
| 骰子、UI 框、箭头、指示器 | 一律不画 |

**判据一句话**：把这张图给一个**不知道这是卡牌游戏**的人看，他不该看出这是卡牌游戏。画面里只要读得出「界面」或「规则」的痕迹，就是越界。

**与既有条款的关系**：

- `no text, no border` 管的是**字与边框**；本条管的是**规则符号与界面元素**，两条一起用。
- 「5 费特效 / 光环」放开的是**渲染手法**（允许柔和的衰减与亮度梯度），**不是内容**——光环不能因此变成法力水晶、能量条或卡牌虚影。
- 「前缀母题」（符文、触手、齿轮、卷轴、星点……）本来就是世界观内的东西，**不受本条限制**；`灵能` 的符文环、`神灵画卷` 的卷轴照旧可以画。

**出图前后各过一遍**：出图前，把提示词里**内容层**的这类词（`card / deck / hand / draw / mana / cost / slot / token`）逐个换成世界观说法 —— **基准提示词前缀是画风定义，照旧一字不改**（`refined hand-drawn card game illustration` 指的是画风来源，不是画面内容）；出图后再问一次「这张图像卡牌游戏吗」。

> **首个适用用例：`01520 神秘商人`**（trait「退场：摸一张牌」「在场时己方手牌召唤物召唤费用 -1」）。画法：**「摸一张牌」→ 从柜台下抽出一张货单递过来**；**「召唤费用 -1」→ 柜台上立着被划掉重写的小价牌 + 一架铜秤**。全程不出现任何卡牌形状。
>
> **待复核的既有卡图**：`01521 辉煌法师` 的「飞舞残页」是按**书本残页**画的（不是卡牌），目前判定不越界；若日后读起来像卡牌，再单独处理。
>
> **2026-09-25 复核出的越界卡（已进重出队列）**：`01316`（腰带里插着一张青绿卡牌 + 两个挂钩）、`01320`（礼帽男抛掷扑克牌）、`01348`（按着桌上的一张绿卡）—— 三张画面里直接出现了**卡牌形状**，违反本条。

### 气质层（Tone）：画风统一，气质随角色

**画风和明度结构是所有卡铁的纪律，「黑暗」不是。** 不是每张卡都该画成黑暗的——同一套平涂语言下，气质按角色走。

| 层 | 是否统一 | 说明 |
|---|---|---|
| 画风 | **统一** | 杀戮尖塔 2、平涂赛璐璐、干净线条、低细节密度 |
| 明度结构 | **统一** | 2–3 级硬边阶梯 |
| 气质 | **随角色** | 黑暗 / 中性 / 优雅 / 神圣 |
| 点缀色 | **随角色或前缀** | 全身只一个高饱和色，色相见下面「前缀配色」 |

- 黑暗系（渊、血歌一类）：凶狠、阴沉、压迫
- 中性系（无前缀普通卡）：平静、日常、不带属性
- 优雅系（舞者一类）：从容、轻盈、有姿态
- 神圣系（神灵画卷一类）：庄严、宁静、可以带一点希望感

生成时把气质词加进**角色描述**（基底里的 `dark fantasy` 不动）：

```
黑暗卡    dark, grim, unsettling
优雅卡    graceful, elegant, light
神圣卡    sacred, serene, luminous
中性卡    不加气质词，按角色描述直写
```

**边界**：气质只允许动表情、姿态、光感、点缀色的明度倾向；**线条粗细、平涂阶梯级数、细节密度一律不动**。`luminous` 指的是少量高亮色块，**不是**辉光、AO 或渐变——**1–3 费**的神圣卡也不许加光晕（5 费按下面「5 费特效 / 光环」条另算；**2026-09-25 晚修：那条自即日起对全档放开，1–3 费同样可按它加光环 / 符文环绕 / 能量丝**）。

> **肤色不随光暗变（2026-09-14 定）**：人形角色的**肤色是角色属性，不是光照属性**——主体站在暗场 / 阴影里时，皮肤仍要有**明确的浅色块**，不许被暗场压成深色或黑皮。暗场只压**衣物与环境**，不压肤色。生成时必须把肤色**写进正文**（如 `warm pale skin`），别指望模型自己守住：01502 首轮三条候选就是「舞者在暗场」这半句 + 低饱和灰紫把整身压暗，读成黑皮。**2026-09-26 补**：肤色同时**不受「低饱和 / 全身只一个高饱和点缀色」约束** —— 它不算那一个点缀色，也不跟着前缀压灰（实测库内「暖色皮肤」占比：1 费 0.4% / 3 费 0.1% / 5 费 3.2%，参考 9.5%）。

### 费用档位与画面体量（Cost tier）—— 2026-09-25 晚修：1–3 费不再受限

**同一套平涂语言下，费用允许拨六个旋钮：体量、元素数、母题程度、细节密度、配色饱和度、服装设计感。** **2026-09-25 晚修：六个旋钮对 1–3 费全部解除限制** —— 「低饱和 / 服装简单 / 明度 2 级 / 细节最克制 / 平实构图」这套档位纪律不再适用；**费用只用来定画面规模**（体量、元素数、母题程度）与占幅参考，细节密度、配色饱和度、服装设计感**按角色定**。画风、2–3 级硬边阶梯、线条粗细、点缀色的**色相**仍与费用无关；图若只是「更立体 / 更亮 / 更写实」，照样算跑偏。（5 费照旧另看「5 费（传奇）档」那一套放开。）

| 旋钮 | 说明 |
|---|---|
| 体量 | 主体在卡面的相对大小、剪影的「重量」与压迫感 |
| 元素数 | 主体个数、随从 / 环境物件数、母题系统的组数 |
| 母题程度 | 属性前缀的显化程度（机械已有先例：低费局部 → 中费半身 → 高费全身，见「机械前缀细化」） |
| 细节密度 | **按角色定**（2026-09-25 晚修：不再按费用递增）。**上浮的是结构线与母题组数，不是渲染手法** |

**分档规范**（占幅 = 1152×1536 卡图里 alpha bbox 的占比，宽 × 高）

> **2026-09-25 晚修**：下表对 1–3 费**只保留「主体 / 元素预算 / 占幅」作画面规模的参考**；「姿态 / 视角 / 构图 / 配色 / 服装 / 明度阶梯 / 内部结构线」这几行**对 1–3 费不再构成限制**（与 5 费同规，按角色定）。

| 项 | 1 费 | 3 费 | 5 费 |
|---|---|---|---|
| 主体 | 单个个体（含小体型、残片、杂兵） | 单个「强者」主体，或主体 + 一个配件 / 随从 | 场域级：庞大单体，或主体 + 群 / 主体 + 环境层 |
| 元素预算 | 1 主 + 0–1 配件 + ≤2 点缀 | 1–2 主 + 1 配件 + 2–3 点缀 | 1 主 + 1–2 支撑层 + 2–3 点缀 |
| 占幅 | 宽 0.60–0.90、高 0.70–0.95 | 宽 0.80–1.00、高 0.80–1.00 | 宽 0.85–1.00、高 0.90–1.00，填充率 ≥0.60 |
| 姿态 | 不限 | 不限 | **要有张力**——动态与站定都成立，见「5 费（传奇）档」 |
| 视角 | 不限 | 不限 | **各种视角**（仰视 / 俯视 / 侧面 / 背视 / 斜角 / 特写） |
| 构图 | 不限 | 不限 | **有张力 / 戏剧性** |
| 配色 | 不限 | 不限 | **可以鲜艳**（不受低饱和限制，按角色定） |
| 服装 | 不限 | 不限 | **有设计感，不能单衣**（多层 / 装饰 / 符合角色特性） |
| 明度阶梯 | 不限 | 不限 | 2–3 级（不变） |
| 内部结构线 | 不限 | 不限 | 最多，见下节 |

**母题程度随费用递进**——一律加**规模与组数**，不加零件个数：

| 前缀 | 递进方式 |
|---|---|
| 灵能 | 晶核层数、符文数量、连接线的条数与长度 |
| 渊 | 触手 / 眼睛的数量与规模、旋涡的范围 |
| 机械 | 局部 → 半身 → 全身（已是规则，见该节；顶级档暂无对应卡） |
| 血歌 | 暂无卡，规则留空 |
| 神灵画卷 | 卷轴展开的规模、星图范围、符文完整度 |
| 无前缀 | 无母题可加，靠体量、元素数与姿态体现 |

**验收**：把 1 / 3 / 5 费各一张缩到卡面尺寸并排，应当读成「小个体 / 强者 / 大物」，装饰与结构线也应当逐档变多（3 费比 1 费细、5 费最华丽）。**唯一不许变的是渲染手法**：不许读成「更写实 / 更立体」，不许出现渐变、体积渲染（**5 费除外**：主体周围的光环 / 特效见「5 费特效 / 光环」条）。姿势 / 视角 / 构图 / 配色 / 服装同理逐档放开：3 费仍常规、低饱和、衣着简单；5 费应读得出张力或戏剧性，且配色可鲜艳、服装要有设计感。

**实测现状（2026-09-13 扫 `Cards/Summon/Hero/{1,3,5}`）**

| 档 | 新基准张数 | 实测 bbox 占幅（中位 / 单张） | 备注 |
|---|---|---|---|
| 1 费 | 28（仅 01130 仍是旧规格） | 宽 0.78 × 高 0.89，填充 0.55 | 上表 1 费那行就是这批的中位，可信 |
| 3 费 | **38 / 41**（1152x1536） | 0.86 × 0.87，填充 0.48 | 余下 01343（2000×2666）、01348（1800×2400）仍是旧规格 |
| 5 费 | **1**（01523 水墨） | 0.48 × 0.91 | 01514 勇者仍是旧规格（2000×2666） |

> **5 费那行的占幅带仍是提案，3 费已转为实测。** 3 费现有 38 张（中位 0.86 × 0.87），提案带基本成立；但同档里 01306 阴（0.65）、01307 阳（0.67）、01334 集群意识（0.63）、01318 弱化棱晶（0.73）、01326 陷阱（高 0.48，躺地的机关天生又宽又扁）明显偏窄——**小巧器物、多体同框同样成立**，要守的始终是「体量 / 压迫感」，不是数字本身。5 费样本仍只有 01523 一张，且只有 0.48 宽（竖长的场域物），5 费不等于占满宽。
> **补注（2026-09-16 复核）**：上表是 2026-09-13 的实测快照，数字此后只作历史读 —— 1 费 29 张里只剩 **01130** 仍是 1800×2400 旧规格（其余全 1152×1536）；3 费已到 **45 张、全部 1152×1536**（01343 / 01348 已换新规格）；**5 费 30 张、全部 1152×1536**。另有一条口径变化：5 费自 2026-09-14 起允许**满幅背景**（**2026-09-25 已作废**，见「半背景（衬托）画法」条），那类图整幅不透明、alpha bbox 恒为 1.00 × 1.00，**「alpha bbox 占幅」这一列对带背景的 5 费已不适用**，别再当门槛用。全库 Hero 型召唤只剩 **01339（3 费）** 一张没有卡图。

#### 5 费（传奇）档（2026-09-12 定；2026-09-13 修：放开配色与服装；2026-09-14 修：3 候选 + 故事感 + 特效 / 光环）

**5 费 = 传奇。** 一句话：**1 费克制、3 费升级、5 费放飞——但画风骨架不变。** 细节密度与装饰量在三档里递进（1 → 3 → 5），5 费是上浮幅度最大的一档。

| 项 | 规则 |
|---|---|
| 画风基准 | **仍守**：杀戮尖塔 2、平涂赛璐璐、干净圆润线条、2–3 级硬边明度阶梯 |
| 背景 / 透明底 | **透明底对全部费用档生效**（2026-09-25 起 5 费不再例外）：四角 + 内缩边带必须透明，背景只能作「衬托物」出现在画窗中央，详见「半背景（衬托）画法」条 |
| 细节密度 | **允许略高于 1 / 3 费**（传奇值得更多细节） |
| 装饰 | **允许加华丽装饰**（华服、光环、庆典、命运纹样……） |
| 服装 | **必须有设计感，绝不能是 1 / 3 费那样的纯色朴素单衣**——多层结构（外套 / 内衬 / 束带 / 披挂）+ 装饰（纹样 / 镶边 / 扣件 / 垂饰），按角色特性设计；不许破布、不许随便套一件、**不许单一纯色平衣** |
| 配色 | **可以鲜艳**——不再受低饱和限制；华丽卡可金 / 紫 / 红，黑暗卡可深色配高对比点缀，完全按角色定 |
| 气质 | **完全按角色特性定**，不设默认：华丽 / 黑暗 / 宏大 / 神秘都成立 |
| 前缀 | **仍按前缀定色相与母题**；5 费放开的是**饱和度与色数**（见「前缀配色」的「多色松绑」条）与母题的**规模与显化程度** |
| 线条 / 明度阶梯 | **不动**：线条粗细统一、阶梯仍是 2–3 级硬边、不用渐变 |
| 特效 / 光环 | **允许主体周围出现装饰性元素**（光环 / 粒子 / 能量 / 气场 / 环境 / 命运线），详见下面「5 费特效 / 光环」条 |

**5 费出图流程（2026-09-14 定）：3 候选 / 有故事感 / 三张明显不同。**

| 项 | 规则 |
|---|---|
| 候选数量 | **每张 5 费卡出 3 个候选**（1 / 3 费仍是每张 2 个候选封顶） |
| 故事感 | **必须有叙事**——① 在做什么（战斗 / 施法 / 守护 / 牺牲）；② 什么状态（巅峰 / 濒死 / 爆发 / 沉思）；③ 什么情绪（愤怒 / 悲壮 / 狂喜 / 威严）。三条里至少读出两条 |
| 差异化 | 3 个候选必须**明显不同**——姿势 / 视角 / 构图 / 叙事各走一条路，不许同一个点子换皮 |
| 参考 | 参考其它游戏的同类传奇立绘：万智牌传奇生物、炉石传说传说卡、杀戮尖塔 BOSS 等 |
| 冲突处理 | **与本文档其它条款冲突时，以 5 费档为准** |

四个方向，按角色特性挑，不强制：

| 方向 | 说明 |
|---|---|
| 华丽 | 华服、光环、神圣、庆典感 |
| 黑暗 | 深渊、恐怖、压迫、厄运 |
| 宏大 | 巨大体型、史诗构图 |
| 神秘 | 命运、预言、神性 |

**5 费要的是张力，不是「必须动」（2026-09-13 定；2026-09-26 修）。** 姿势、视角、构图三项在 5 费一并放开，画风骨架（平涂赛璐璐、线条、2–3 级阶梯）一律不动。**同日再放开配色与服装两项**（见下面两小节）——5 费的放开项合起来是**姿势 / 视角 / 构图 / 配色 / 服装 / 特效与光环**六项。

> **2026-09-26 修**：旧口径「**不能只是站着**」把**目的**（张力）写成了**手段**（动）。用户原话：「**五费姿势说是要动态，其目的是要张力，但是参考图这些站立也是很有张力**」—— 实测站定在参考图里占 **19/32**（见「半背景（衬托）画法」的逐轴规则），站定本身就是主调。**本条自此只判张力，不判动**：判据是下面「站定的张力装置」，不是「有没有在动」。

> **2026-09-25 晚修补**：上面「1–3 费照旧克制、只有 5 费放开」的旧口径**作废** —— 姿势 / 视角 / 构图三项**对 1–3 费同样放开**，按角色定（见「费用档位与画面体量」的 2026-09-25 晚修与「半背景（衬托）画法」的逐轴规则）。5 费**单独多出**的只有「**张力门槛更高**」这一条（2026-09-26 修），不是「必须动」。

> **视角不再是 5 费专属（2026-09-13 定）：部分卡可以多视角。** 上表里「1–3 费＝常规视角」降级为**默认值，不是硬约束**——当主体的形态本身要求换视角时（例：蜷成一团的幼龙、盘起来的蛇、俯卧的兽），1–3 费同样允许俯视 / 仰视 / 斜俯等非常规视角，只要画风骨架（平涂赛璐璐、线条粗细、2–3 级阶梯、细节密度、透明底）一律不动。首个用例：**01305「幼生之龙」（3 费，正俯视，睡姿盘成螺旋）**。**2026-09-25 晚修：姿势与构图两项也一并对 1–3 费放开**，见下面那张表。

**六项放开项对全档生效（2026-09-25 晚修）；下表只记 5 费自己的侧重。**

| 项 | 5 费的侧重（1–3 费不据此判退） |
|---|---|
| 姿势 | **要有张力** —— 动态成立，**站定同样成立**；判据见下「站定的张力装置」 |
| 视角 | **各种视角**（仰视 / 俯视 / 侧面 / 背视 / 斜角 / 特写） |
| 构图 | **有张力 / 戏剧性** |
| 配色 | **可以鲜艳** |
| 服装 | **有设计感，不能单衣** |
| 特效 / 光环 | **允许**（详见「5 费特效 / 光环」条；2026-09-25 晚修起对 1–3 费同样放开） |

**可用姿势 · 动态向**（挑一个即可，不强制）：

| 姿势 | 说明 |
|---|---|
| 冲刺 | 向前冲锋 |
| 挥砍 | 武器挥动中 |
| 施法 | 手势 / 召唤 |
| 防御 | 举盾格挡 |
| 咆哮 | 张嘴怒吼 |
| 跪地 | 悲壮 / 濒死 |
| 悬浮 | 漂浮 / 升腾 |
| 扑击 | 向前扑 |

**可用姿势 · 站定向（2026-09-26 定）**：站定要「有张力」，靠下面六个装置，不靠动起来 —— 取自参考图里那 19 张站定。

| 装置 | 做法 | 参考用例 |
|---|---|---|
| **仰角** | 相机低于胸线，身形朝镜头压过来 | `img19` `img24` `img28` |
| **不对称承重** | 重心压一腿，另一腿虚点 / 踩高处（台阶、木箱、残骸） | `img22` `img05` `img29` |
| **躯干扭转** | 3/4 侧身，肩线朝向与胯线不一致 | `img08` `img20` `img27` |
| **对角 / 竖直线** | 长兵器、长衣摆或身体轴本身拉出一条线，与地平线对冲 | `img14` `img21` `img02` |
| **脚下不稳** | 踩在碎石 / 斜坡 / 机械残骸上，不是平地 | `img23` `img24` `img27` |
| **手上有活** | 抬起 / 插兜 / 扶刀 / 按帽 —— 不是两条手臂垂着 | `img05` `img20` `img26` |

> 判据一句话：**把背景遮掉只留人物剪影，这张图仍然「有戏」** —— 有戏就是张力；没戏才叫「只是站着」。1–3 费同样按这条判，不按费用降标准。

**可用视角**：

| 视角 | 说明 |
|---|---|
| 仰视 | 显高大 |
| 俯视 | 显威严 |
| 侧面 | 显动态 |
| 背视 | 显神秘 |
| 斜角 | 显张力 |
| 特写 | 显气势 |

> 「跪地」「悬浮」属**静态**姿势，同样按上面那条判据审（悲壮 / 升腾）—— **5 费的要求是「有张力」，不是「一定在动」，也不是「不普通」地摆造型**。

**5 费服装（2026-09-13 定；2026-09-14 修：纯色朴素单衣一律不合格）：要有设计感，不能是单衣。**

| 要 | 不要 |
|---|---|
| 多层 / 有结构（外套 + 内衬 + 束带 + 披挂……） | 单衣、破布、随便套一件 |
| 装饰（纹样、镶边、扣件、披风、垂饰） | 朴素到读不出「传奇」 |
| 设计符合角色特性（华丽的更华丽、黑暗的更诡异） | 与角色设定无关的堆砌 |
| 与 1 / 3 费拉开档次 | **纯色朴素的单件平衣**——1 / 3 费那种衣服照搬到 5 费，一律不合格 |

> **硬性条（2026-09-14 定）**：5 费衣物**无论如何都不能像 1 / 3 费卡那样是一件纯色朴素衣服**——哪怕配色、姿势、构图都合格，只要衣服读起来是「一件没有层次与装饰的纯色平衣」，这张就不合格。1 / 3 费照旧简单（它们的朴素是分档要求，不是缺陷）。

**边界**：设计感只准落在**衣物的层次与装饰块**上——仍是硬边平涂色块，不许堆到绣花纹样级的密度，不许渐变与体积渲染。加的是**组数与块面**，不是零件个数。

**5 费配色（2026-09-13 定）：可以鲜艳，不再受低饱和限制。**

| 方向 | 说明 |
|---|---|
| 华丽卡 | 金、紫、红等鲜艳色可用 |
| 黑暗卡 | 主体仍可深色，但**可以配高对比点缀** |
| 神圣卡 | 明亮色块可用 |

**边界**：放开的是**饱和度与色数**，不是渲染手法——鲜艳色同样只能是**硬边平涂色块**，不许渐变、体积渲染（特效 / 光环另见下条）。有前缀的卡仍先按前缀定色相；鲜艳到什么程度**完全按角色特性定**（1–3 费照旧低饱和）。

**5 费特效 / 光环（2026-09-14 定）：允许主体周围出现装饰性元素。** 原先「5 费也不许加光晕 / AO 辉光」的写法自此作废——这是**渲染手法**上的一处放开。**2026-09-25 晚修：本条自即日起对全档放开**（1–3 费同样可加光环 / 符文环绕 / 能量丝），不再是 5 费专属。

| 类型 | 例子 |
|---|---|
| 光环 | 能量光环、神性光辉、暗影环绕 |
| 粒子 | 火星、碎屑、尘埃、光点 |
| 能量 | 紫焰、蓝光、绿芒 |
| 气场 | 压迫感、威压波纹 |
| 环境 | 周围的碎石、火焰、雾气 |
| 命运线 | 编织的线、预示的纹 |

| 前缀 | 特效 |
|---|---|
| 渊 | 紫光、触手虚影、旋涡、注视的眼 |
| 机械 | 蒸汽、齿轮虚影、火花 |
| 灵能 | 蓝光、符文环绕、能量丝 |
| 血歌 | 血滴、火焰、音符 |
| 神灵画卷 | 绿芒、星点、卷轴虚影 |
| 无前缀 | 按角色定 |

**边界**：放开的是**主体之外**那一层——光环、光点、火星可以有柔和的衰减与亮度梯度；**主体本身**照旧守平涂赛璐璐（2–3 级硬边、线宽统一，渐变与体积渲染不进门）。特效不准压住剪影：缩到卡面尺寸时外轮廓仍要读得出来。

**5 费背景（2026-09-25 修：2026-09-14 的「允许满幅」作废）。** 5 费**不再允许铺满画布的背景层**——满幅会盖死画窗四角与边带，带前缀的 5 费在战场上会**丢失前缀信息**（`prefixbg-v2/README.md`「已知边界」已记实测）。5 费现在与 1–3 费同规：**四角 + 内缩边带必须透明**，背景只允许作「衬托物」出现在画窗中央。**代价：现存 29 张带满幅背景的 5 费卡全部进入待重出队列。**

| 项 | 说明 |
|---|---|
| 背景 | **四角 + 内缩 46px 边带必须透明**；背景只能作「衬托物」出现在画窗中央（召唤 `x76–692 / y268–1043`） |
| 抠图 | 5 费与 1–3 费同规：加 `--transparent` 出洋红底再抠图，按「抠图注意事项」处理 |
| 提示词 | 结尾照旧保留 `transparent background`；衬托物写在主体描述之后，`3:4 portrait, no text, no border` 照旧 |
| 仍守 | **画风骨架不变**：平涂赛璐璐、干净圆润线条、2–3 级硬边明度阶梯；衬托物照旧走硬边色块语言，不许照片级写实渲染 |
| 剪影 | 衬托物可以让步，**主体的剪影不许被它吃掉**——缩到卡面尺寸时外轮廓仍要读得出来 |

> **示范用例（2026-09-14；2026-09-25 起「满幅背景」部分作废，本节仅作历史记录）：`01502 影舞者`。** 5 费新档的样板张 —— 首张**满幅背景**（斜俯约 55°，画面沿对角线对半切：金色亮台 / 暗紫后场）、首张**金丝网纱罩住五官**（金色额环垂下金线网，网下只有头的软轮廓）、首张按「肤色不随光暗变」处理（冷白皮）、首张走满 **3 候选**（三张明显不同：并肩背身 / 召回影子 / 背靠背）。它的影子是**无描边、边界化进背景**的柔边投影，不是复制的黑色个体 —— 这条对「影子 / 幻影 / 分身」类角色通用（同日按这套语言配了 `03007 影子`：只画影子不画人、站在一根金色射灯光柱里、边缘被光吃掉碎成平的暗色碎片，刻意不加描边与轮廓光。代价是 `hard` 只有 3.1 —— **通篇无描边就等于主动放弃「线宽统一」**，「影子不是个体」与「线宽统一」二者不可兼得，先保前者）。参考它时注意两点：抠图链**跳过**（无透明底），提示词结尾的 `transparent background` 换成背景描述；它的 `hard` 只有 7.3（基准 23.7）、线仍偏软，是这张模型的通病，**不是标准**。

**5 费露脸与五官（2026-09-14 定；2026-09-15 增：女性 5 费卡露脸一律以 `01504 群狼之王` 为脸部生成参考；2026-09-16 修：脸部结构上限放开一档，「三笔」不再是上限；2026-09-25 修：`01504` 提级为**全档脸模基准**）。** **脸模照 `01504` 的二次元处理（全档统一）**；不照搬的是**这个角色自己的骨相与个性**。

| 项 | 规则 |
|---|---|
| 细节量 | **完整结构，不再只留三笔**（2026-09-16 修）：眉（一条硬边粗眉，可下压出情绪）、上睑线 + 上睑褶（一粗一细两条线）、虹膜（**四层**：外环深 + 内部渐变 + 深瞳孔 + 一枚明亮高光）、泪阜、下睑线 + 下睑阴影、鼻（一小块阴影三角或一条极短竖线）、嘴（短横线或小幅弧线，可单侧抬嘴角）、脸颊与下颌 2–3 块硬边分面、耳朵简化成平涂形状。 |
| 脸的画法 | **2–3 块硬边平涂**（受光面 + 阴影面，必要时再加一块下颌下的更深阴影），分界是直边；下颌线连续闭合；脖子与下颌下压一块更深的阴影，让脸从衣领里跳出来。 |
| 头发 | 硬边大块 + 少量尖角分束，不画细发丝。 |
| 肤色 | 照「肤色不随光暗变」条——冷白皮，明度明显高于衣物。 |
| 禁止 | **写实体积那一类**：写实三层眼球（真实眼球体积）、高光球、唇形、牙齿、婴儿肥、面部写实体积。**2026-09-26 二次修**：只放开 **虹膜边缘略深 + 一枚小高光 + 一条浅上睑褶线**；**腮红 / 浓眼线 / 睫毛尖 / 泪阜阴影仍旧禁**（第一次全放开实测出「猎奇脸模」，见「脸模」条）。 |
| **照搬 / 不照搬** | **照搬**：二次元的**样式与比例**（大眼、上睑线 + 浅上睑褶、**虹膜一块基色 + 一枚小高光**、小鼻、小嘴、下颌收窄、刘海压额头）—— **全档统一**，不只女性 5 费。**不照搬**：这个角色自己的**骨相与个性**（脸型、眼型与眼神、表情与年龄感、发型与额饰）一律**按角色重设计**，且同费用档内不许重复；眼里的点缀色按该张的配色定，**不是固定冰蓝**。 |

> 5 费「露脸」的通用口径：5 费允许给表情（属气质层的放开项），给的是**结构完整的五官**的表达力，**不是写实体积渲染**。**2026-09-26 起画法照「脸模」条**（用户 `脸部参考.docx` 31 张），**不再照 `01504`** —— `01504` 只作**细节量的下限参照**（新图的脸不许比它更简单）。它与「平涂细则」的关系见「脸模」条：**脸是平涂的唯一例外**，虹膜渐变 / 睫毛 / 泪阜 / 腮红 / 软过渡**允许**；1–3 费同规。

> **露脸 ＝ 脸部参考（2026-09-15 定，首个用例 `01509 恐怖分子`；2026-09-26 改：基准换成用户 `脸部参考.docx` 31 张，`01504` 退出）。** 只要露脸，脸部一律照「脸模」条的七件逐项写。注意：现行生成线路（`ofoxai` / `qwen/qwen-image-3.0-pro`）是**纯文本出图、没有图像输入参数**，「喂参考图」这条做不到 —— 实际做法是把「脸模提示词片段」**整段照抄**进提示词正文。

| 部位 | 画法（2026-09-26 起，全档统一） |
|---|---|
| 眼 | **上睑线**：一条细到中等的深色线，贴住眼上缘，**外眼角收成一个短而细的尖**（**不外飞、不画独立睫毛尖**）；**上睑褶**：一条**细而浅**的线；**上睑只压住虹膜上缘一小段**（虹膜读成杏仁）；**虹膜**＝一块基色 + 边缘一圈略深 + 中间略亮 + **一枚小高光（约虹膜 1/10，偏上）**；**下睑**：一条细浅线；**不要**大白眼珠 / 深瞳孔点 / 洋娃娃高光 |
| 眉 | 一条细眉，**与上睑线之间留出明显皮肤间隙**（不许贴上睑线） |
| 鼻 | 一个点，或一条极短竖线 |
| 嘴 | 一条短横线，**不画下唇**；表情靠弧度与开口 |
| 脸 | 底色 + **刘海压住额头与眼上缘并投下一条阴影带** + 下颌 / 颈侧阴影；**不画腮红**；下颌线连续闭合 |
| 肤色 | **暖白 / 浅奶油**，明度明显高于衣物与头发，**且不受低饱和约束**（见「脸模」条硬约束 3） |
| 发 | 大块 + 少量尖角分束，可留一条高光带 |

> **照搬 / 不照搬**（与上面那一行同规）：**照搬** = 二次元的**样式与比例**（大眼、上睑线 + 浅上睑褶、虹膜一块基色 + 一枚小高光、小鼻、小嘴、下颌收窄、刘海压额头），**全档统一**；**不照搬** = 这个角色自己的**骨相与个性**（脸型、眼型与眼神、表情与年龄感、发型与额饰）**一律按角色重设计**；眼里的点缀色按该张的配色定，**不是固定冰蓝**。
>
> **与上表「禁止」行的关系（2026-09-16 立，2026-09-26 两次改）**：那行原本连**面部软过渡 / 虹膜渐变 / 腮红 / 睫毛**一起禁。**2026-09-26 第一次改**把它们全部解禁（⇒ 实测出「猎奇脸模」）；**同日第二次改收窄**：只放开 **虹膜边缘略深 + 一枚小高光（约虹膜 1/10）+ 一条浅上睑褶线**，**腮红、浓眼线、睫毛尖、泪阜阴影重新禁**。那行继续禁**写实体积那一类**（真实眼球体积、高光球、唇形、牙齿、婴儿肥）。画法与参考一律照「脸模」条。
>
> **适用范围**：上面这张**细化表**只约束**女性 5 费卡露脸**；男性 5 费卡与非人形主体**不适用这张表**（按各自角色处理），1–3 费走「1–3 费的脸：少而结构化」那条、不进这张表。**但「脸模 = `01504` 的二次元处理」那一条是全档的**（2026-09-25 定）：男性 5 费与 1–3 费的人形角色一样照它。

**仍守**：① 画风基准（杀戮尖塔 2 + 平涂赛璐璐）；② 透明底（**全档适用**，见「半背景（衬托）画法」条）；③ **前缀仍定色相与母题**——有前缀的卡仍取该前缀的母题，色相仍以它为准；5 费放开的只是**姿势 / 视角 / 构图 / 配色（饱和度与色数）/ 服装 / 特效与光环**这六项。

**边界（仍不许越）**：上浮的是**装饰量、母题组数与色数**，不是渲染手法——依旧不许喷枪渐变、体积渲染、写实解剖、交叉排线（**特效 / 光环是唯一例外**，见上面「5 费特效 / 光环」条）。**鲜艳配色不等于放开渲染**：**主体上**的饱和色只能以硬边平涂色块出现，不许用渐变 / 辉光去「提亮」。5 费图若读成「更精致 / 更立体 / 更亮」，仍然算跑偏。

### 前缀配色（Prefix palette）

总画风（杀戮尖塔 2 + 暗黑奇幻低饱和；**5 费可鲜艳**，见「5 费（传奇）档」）**所有卡牌统一**。前缀只改变**色调、元素、默认气质**三项，不改变画风；差异一旦破坏统一感，就是跑偏。表里的「气质」只是该属性的**默认**气质，角色描述可以带出更具体的气质（优雅 / 中性 / 神圣），以角色为准，见上面「气质层」。

| 前缀 | 主色 | 气质 | 视觉特征 |
|---|---|---|---|
| 灵能 | 蓝 | 精神、联结 | 符文、水晶、连接线（细化见下） |
| 渊 | 紫 | 阴沉、深渊 | 触手、眼睛、旋涡（细化见下） |
| 机械 | 棕 | 厚重、构造 | 齿轮、金属、蒸汽（细化见下） |
| 血歌 | 红 | 狂热、牺牲 | 血滴、音符、燃烧 |
| 神灵画卷 | 绿 | 神秘、古卷 | 书籍、卷轴、星点（细化见下） |
| 无前缀 | **不固定** | 中性、普通 | 简洁、无属性标志 |

> **`无前缀` 的「灰」说的是气质，不是色相。** 它不锁定点缀色——点缀色**按角色描述逐张决定**，可以红、可以紫、可以金绿。它和有前缀卡的区别只有一条：**不带属性母题、不套属性配色**。不要把它理解成「必须画成灰的」。

**与「唯一高饱和点缀色」的关系**：前缀不是那条规则的例外，而是**指定它的色相**。上面「全身只允许一个高饱和点缀色」照旧成立——有前缀的卡取表里的「主色」；**`无前缀` 卡不套任何属性配色，点缀色由角色描述决定**。不论哪种，其余部分仍然全部压在低饱和灰黑。

**前缀只决定两件事**：① 唯一点缀色的色相；② 一到两个属性母题。**明度结构、线条、平涂阶梯、细节密度一律不动**——差异只允许落在这两处，这就是「有差异但不破坏统一感」的界线。

> **5 费（传奇）档另放开姿势 / 视角 / 构图 / 配色 / 服装**（见「5 费（传奇）档」）——那是**画面调度与配色**的放开。母题照旧，**点缀色的「只准一个」在 5 费不成立**（见下面的「多色松绑」条）。

**双前缀（2026-09-12 定）：两套风格结合。** 全库唯一一张双前缀卡是 1332「异面信徒」（字段 `渊 机械`）。画法是**两个前缀的母题与材质一起上**——渊的触手 / 眼睛 / 旋涡 + 机械的齿轮 / 金属板 / 蒸汽，不是二选一。但**「全身只允许一个高饱和点缀色」这条不变**：点缀色取**字段里靠前的那个前缀**（`渊 机械` → 深紫），另一个前缀只贡献母题与材质，它的颜色（黄铜 / 暗钢）本来就压在低饱和，不构成第二个点缀色。

生成时，在基准提示词前缀之后、角色描述之前，追加该前缀的一行：

```
灵能      psychic blue accent, glowing runes and crystal shards, thin connecting energy lines, being of pure psychic energy
渊        deep purple accent, writhing tentacles, watching eyes, swirling abyss
机械      rust brown accent, exposed gears and metal plating, venting steam
血歌      blood red accent, falling droplets and burning embers, faint musical notes
神灵画卷   mystic green accent, ancient scrolls and tomes, scattered star motes
无前缀    no attribute motif and no attribute palette, plain and grounded; take the single accent colour from the character description itself
```

完整提示词 =

```
<基准提示词前缀>
<该前缀的修饰行>
<气质词（dark, grim, unsettling ｜ graceful, elegant, light ｜ sacred, serene, luminous；中性卡可省）>
<角色描述>
<机位>                ← 默认俯视：slightly high camera angle, the viewer looks down at the character
<脸模提示词片段>      ← 2026-09-26 起放这里：紧接角色描述，人形角色必写
<衬托物 / 背景描述>
<结尾复述：脸的要求最短版>   ← 例：anime face, almond eyes, thin lash line with a short outer point, faint crease line, one small iris highlight, bangs over the eyes, no blush, skin clearly lighter than the clothes
transparent background, 3:4 portrait, no text, no border
```

**为什么脸模要提前 + 结尾复述（2026-09-26 定）**：模型对长提示词的**开头与结尾**注意力最强、中间最弱。旧拼法把脸模夹在正中间，等于放进最不被看见的位置。现在两处都点名：正文写完整版，结尾用短版复述一次。

**数据实况（2026-09-11 核对 `Assets/_Game/Resources/CardData`）**

- 前缀字段实际只出现 `无`（138 张）、`渊`（14）、`机械`（11）、`灵能`（11）、`神灵画卷`（3）、以及双前缀 `渊 机械`（1 张，见上面「双前缀」条）
- **`血歌` 目前一张卡都没有**。规则先记着，等有卡再用
- 参考卡 01103「腐化之心」是 `渊`，所以它的紫色点缀与该规则一致

**`无前缀` 卡的点缀色不受本表约束**：01101 匕首的暗红、01104 卷轴/皮具的暖棕、01105 的紫色大衣、01107 的金绿翅膀，都是按各自角色描述定的，符合规则，**不需要为了「配灰」去改**。

> **「只准一个高饱和点缀色」的松绑（2026-09-13 定，同日扩）。** 「全身只允许一个高饱和点缀色」只管**画面统一感**，不是不可越的铁律：**部分 3 费卡可以无视它，5 费卡更不必守**（5 费本就是传奇档，配色按角色走）。判据是**角色本身需不需要多色**——需要就上，不需要就仍旧单点缀色；**不要为了「多色」而多色**，也不要把多色做成「什么颜色都来一点」。
>
> 起因是 **01323 法官**：角色描述本身要求「鲜艳的红色条带 + 金色天平」两个饱和色，照旧规只能做单色，按此条放开。目前真正用上多色落盘的仍只有 **01301 小丑**（丑角本身多色 → 红 / 黄 / 钴蓝 / 白同框，落盘实测平均饱和 193.7，与基准 01103 的 101.4 同量级）；01323 定稿反而选了**红 + 低饱和暗黄铜**的克制版（亮金版 `01323b` 已生成、未采用）——**放开的是「允许」，不是「必须」**。
>
> **01310 蜂王（2026-09-13）另开一型：点缀色升成「整体基调色」。** 这张不是「多色」，而是把那个单点缀色放大到**主体整片**——`前缀: 无`、3 费，角色是蜂后，定调「整体是黄色的」，于是甲壳整片走蜜黄 + 深褐两级硬边（落盘实测平均饱和 160.7、luma 121.5，明显高于基准 01103 的 101.4 / 71.2）。同样是「按角色需要」的放开：**主体可以整体偏某个饱和色相，但 2–3 级硬边阶梯、线宽、细节密度、透明底照旧不动**。
>
> 松开的**只有「饱和色的数量」这一项**：平涂赛璐璐、2–3 级硬边阶梯、线宽统一、细节密度、透明底一律不动；**有前缀的卡仍要先用完前缀给的那个色相**，多出来的饱和色必须由**角色本身的设定**要求（丑角的多色戏服、法官身上的金天平），不能凭空加。

### 机械前缀细化（改造程度分级）

上面表里「机械 = 棕 / 厚重、构造 / 齿轮、金属、蒸汽」只是简写（内容与本表一致），机械卡的具体画法按本节走。

**核心概念：肉体机械改造。** 不是纯机械，是**血肉与机械的结合**；改造程度随卡牌强度（费用）递进。前缀的两条老规矩照旧——① 唯一点缀色 = 锈棕；② 属性母题取下面这几个；线条粗细、平涂阶梯级数、细节密度一律不动。

**统一母题**（机械前缀只允许这五个）

| 母题 | 说明 |
|---|---|
| 齿轮 | 外露的精密齿轮 |
| 金属板 | 黄铜 / 钢铁装甲片 |
| 蒸汽 | 关节处喷出的蒸汽 |
| 管线 / 铆钉 | 机械与肉体的接口 |
| 接缝 | 血肉与金属的连接处 |

**配色**

| 项 | 值 |
|---|---|
| 点缀色 | **锈棕（rust brown）**，全身仍只此一个高饱和色 |
| 主体 | 低饱和灰黑 |
| 金属 | 黄铜 / 暗钢 |

**改造程度分级**（按费用填进角色描述）

| 强度 | 改造程度 | 形象 |
|---|---|---|
| 低费 | 少量（局部） | 大部分肉体，一两处机械（义肢 / 义眼 / 颌） |
| 中费 | 半身 | 上半身机械，齿轮外露，肉体接口 |
| 高费 | 全身 | 几乎纯机械，保留人形轮廓 |
| 顶级 | 完全体 | 精密构造，庞大压迫 |

**数据实况（2026-09-12 核对 `Assets/_Game/Resources/CardData`）**：机械卡共 12 张（11 张纯机械 + 1332 双前缀），费用只落三档，对应关系是——

- 费用 0–1（tier 1）→ **低费**：3004 杂兵、1114 暴徒、1118 猎犬、1125 投机者
- 费用 3（tier 2）→ **中费**：1308 麻烦制造者、1328 破防者、1345 改造人、3005 飞升者、1332 异面信徒
- 费用 5（tier 3）→ **高费**：1505 封锁者、1506 增幅结构、1513 复生造物
- **顶级目前没有对应卡**：全库最高费用就是 5，这一级先留着

**卡图现状（2026-09-13）**：12 张里有 6 张有图——低费档 01114 暴徒、01118 猎犬、01125 投机者（都在 `Cards/Summon/Hero/1/`，能正常加载），中费档 01308 麻烦制造者、01328 破防者、01332 异面信徒（`Cards/Summon/Hero/3/`）。其余 6 张为空：01345 改造人（3 费），01505 封锁者、01506 增幅结构、01513 复生造物（5 费），以及 03004 杂兵（0 费）、03005 飞升者（3 费）——**最后两张是 `summonType: 2` 的 Special 型召唤**，卡图目录由 `summonType` 决定、不能照抄 CardData 的目录，见「落盘规范」。
**卡图现状（2026-09-16 复核：机械 12 张已全部有图）**：低费档 01114 暴徒、01118 猎犬、01125 投机者（`Cards/Summon/Hero/1/`）；中费档 01308 麻烦制造者、01328 破防者、01332 异面信徒、01345 改造人（`Cards/Summon/Hero/3/`）；0 费 03004 杂兵与 3 费 03005 飞升者（两张都是 `summonType: 2 (Special)`，卡图在 `Cards/Summon/Special/`）；5 费 01505 封锁者 / 01506 增幅结构 / 01513 复生造物（`Cards/Summon/Hero/5/`）——**机械组自此无空位**。上面那段「（2026-09-13）」是当时的快照：01345 / 03004 / 03005 已于 2026-09-14 补图，01505 / 01506 / 01513 于 2026-09-16 补图；**该段末尾的目录提醒仍然有效**——Special 型召唤的卡图目录由 `summonType` 决定、不能照抄 CardData 的目录，见「落盘规范」。

**气质**：机械力量感——厚重、精密、构造感。**不黑暗**，偏工业 / 精密美学。这不是「气质层」的例外：它同样只动表情、姿态、光感、点缀色明度倾向，不动线条粗细、平涂阶梯级数、细节密度。因此机械卡默认**省掉气质词**，把「工业 / 精密」写进角色描述即可。

| 要 | 不要 |
|---|---|
| 齿轮 / 金属板 / 蒸汽母题 | 纯人无改造 |
| 血肉与机械结合 | 纯机械无人形 |
| 精工装备 | 破烂废铁 |
| 低细节密度（机械抽象成简单形状） | 堆砌精密零件 |

**负面词**（**已实测 2026-09-12：`ofoxai` 不接收 `--negative`，传了会被静默忽略**——下面这串必须写成正文里的否定句）

```
rusty, broken, scrappy, junkyard, pure machine, no human form, photorealistic, overly detailed, complex, CG render, 3D, busy background, watermarks
```

**完整提示词（机械卡）**

```
<基准提示词前缀>
rust brown accent, exposed gears and metal plating, venting steam
<角色描述 + 对应改造程度>
transparent background, 3:4 portrait, no text, no border
```

### 渊前缀细化（深渊造物）

**核心概念：深渊造物。** 从深渊中来的存在——阴沉、诡异、**被注视**。不是纯怪物，是「深渊的具现」。前缀的两条老规矩照旧：唯一点缀色 = 深紫；母题取下面这几个。

**统一母题**（渊前缀只允许这五个）

| 母题 | 说明 |
|---|---|
| 触手 | 蠕动的触手 |
| 眼睛 | 注视的眼睛（单个 / 多个） |
| 旋涡 | 深渊的旋涡 |
| 深渊裂缝 | 从深处裂开的口 |
| 暗紫能量 | 从深处涌出的能量 |

**配色**

| 项 | 值 |
|---|---|
| 点缀色 | **深紫（deep purple）**，全身仍只此一个高饱和色 |
| 主体 | 低饱和灰黑 |
| 能量 | 暗紫 / 幽紫 |

**气质**：阴沉、诡异、被注视感——比机械更黑暗，但仍然是「神秘的压迫」，**不是血腥恐怖**。对应「气质层」就是黑暗系那一档（`dark, grim, unsettling`）。

| 要 | 不要 |
|---|---|
| 触手 / 眼睛 / 旋涡母题 | 纯人形无渊系元素 |
| 深紫点缀色 | 明亮色 |
| 阴沉诡异 | 血腥恐怖 |
| 被注视的压迫感 | 可爱 / 友好 |
| 低细节密度 | 堆砌器官纹理 |

**负面词**（**已实测 2026-09-12：`ofoxai` 不接收 `--negative`，传了会被静默忽略**——下面这串必须写成正文里的否定句）

```
bright colors, cute, friendly, gore, bloody, anatomy, muscle definition, ribs, striations, exposed sinew, photorealistic, overly detailed, complex, CG render, 3D, busy background, watermarks
```

**完整提示词（渊卡）**

```
<基准提示词前缀>
deep purple accent, writhing tentacles, watching eyes, swirling abyss
dark, grim, unsettling
<角色描述>
transparent background, 3:4 portrait, no text, no border
```

**数据实况（2026-09-12 核对 `Assets/_Game/Resources/CardData`）**：渊系卡 15 张（14 张纯渊 + 1332 双前缀），费用 0 / 1 / 3 / 5 四档都有。

已有卡图 10 张——全部按基准规格（1152x1536）：01102 堕落者（跪姿紫袍 + 触手）、01103 腐化之心（基准卡，心口紫光）、01115 虚伪之火（紫焰空壳 + 虚空独眼）、01116 肉山（锥形肉山 + 群眼 / 紫涡底盘）、01117 苦难给予者（旋涡中伸出）、01302 疗（跪坐按地 / 掌侧紫雾）、01303 潜行者（低伏四足蹲踞 / 前肢散成触手束，点缀色在触手尖端与眉心裂缝）、01332 异面信徒（双前缀，机械主体 + 渊母题，见「双前缀」）、01317 贪欲之蛇（盘成三圈的巨蛇 / 盘心独眼 / 身下旋涡，2026-09-13，由 1800x2400 的恶搞占位稿改为正式插画）、01331 囚牢（新增卡图，2026-09-13；骨肋拱笼 / 锁死的大门上一只竖瞳独眼 / 笼内暗紫旋涡 / 粗绷锁链与底部深渊裂缝）。

暂无卡图 5 张：3002 幽魂、1304 黑暗骑士、1503 领主、1526 忤逆者、3003 被腐化者。

### 灵能前缀细化（能量体）

**核心概念：能量体。** 灵能生物**全部能附着、也能独立放置**，所以它们不是血肉实体，是**能量的凝聚**。**半透明只是质感倾向，不是必须真透**——一句话：**灵能 = 实色平涂 + 能量感表现**。

| 特征 | 说明 |
|---|---|
| 主体 | 由能量构成，不是血肉；**照常画实色平涂，alpha 保持 1** |
| 质感 | 边缘发光、内部有能量流动感、轻盈，不像实体那么重 |
| 气质 | 精神体、非实体 |

**这是灵能与其他前缀最大的区别**——其他前缀是血肉 / 金属实体，灵能是**能量的凝聚体**。

**统一母题**（灵能前缀只允许这五个）

| 母题 | 说明 |
|---|---|
| 符文 | 精神印记 |
| 水晶 | 菱形水晶 / 碎片 |
| 连接线 | 意识的链接 |
| 能量感 | 边缘发光、内部能量流动 |
| 光晕 | 精神的微光（一小块高亮平涂，不是 glow） |

**配色**

| 项 | 值 |
|---|---|
| 点缀色 | **蓝（psychic blue）**，全身仍只此一个高饱和色 |
| 主体 | **蓝色主调**：能量体本身走低饱和蓝 / 蓝灰，**不是灰白**（实色，不靠降 alpha 做通透） |
| 能量 | 淡蓝 / 青光 |
| 整体 | 整张卡读起来应该是**蓝的**——「主色调是蓝色」 |

> **主色调是蓝（2026-09-12 定）。** 灵能卡的色彩印象一律偏蓝：能量体的主体、边缘高光带、符文 / 水晶 / 连接线全部落在蓝色系里，**不许出现灰白主体的裸感**。蓝的明度阶梯照常 2–3 级硬边，只有一处可以用高饱和蓝（水晶核 / 符文 / 线尾节点）。

> **实色平涂，不是真透明。** 「能量感」只靠**边缘发光、内部高亮色块、整体提亮**暗示，色块本身照常是硬边平涂、明度阶梯照常 2–3 级。**不许**用渐变、雾化、AO 辉光（**5 费例外**：主体周围可按「5 费特效 / 光环」条加光环 / 符文环绕 / 能量丝），也**不许**把主体真做成半透明（卡面底是深色，真透明会透出卡底，且难平涂）。所谓「光晕」只是一小块高亮平涂。缩到卡面尺寸时剪影仍要读得出来。

**气质**：空灵、精神、能量感——不是黑暗，是「能量的凝聚」。默认**省掉气质词**，写进角色描述即可。

| 要 | 不要 |
|---|---|
| 能量体（实色平涂 + 能量感表现） | 真 alpha 半透明 / 通透到剪影读不出 |
| 符文 / 水晶 / 连接线 | 无灵能母题 |
| 蓝色点缀 | 暖色 |
| 空灵精神感 | 沉重血肉实体感 |
| 低细节密度 | 堆砌细节 |

**负面词**（同上：**必须写成正文里的否定句**）

```
translucent, transparent body, see-through, ghostly, heavy, earthy, bright colors, photorealistic, overly detailed, complex, CG render, 3D, busy background, watermarks, anatomy, muscle definition, ribs, smooth mannequin, nude body
```

**完整提示词（灵能卡）**

```
<基准提示词前缀>
psychic blue accent, glowing runes and crystal shards, thin connecting energy lines, being of pure psychic energy
<角色描述>
transparent background, 3:4 portrait, no text, no border
```

**数据实况（2026-09-12 核对 `Assets/_Game/Resources/CardData`）**：灵能卡 11 张（1126 凝聚体、1127 改造者、1128 超数故障、1129 滋养者、1333 幻身、1334 集群意识、1335 能量骇客、1336 修正者、1527 消逝之影、1528 能量收割者、3027 中枢），费用 0 / 1 / 3 / 5。

**已有卡图 9 张**：01126 凝聚体、01127 改造者、01128 超数故障、01129 滋养者（均 2026-09-12 前后按基准规格 1152x1536 出图）、01333 幻身（2026-09-13，残像叠影画法）、01334 集群意识（2026-09-13，多体同框画法，替换掉原 2000x2666 旧稿，旧稿归档 `Art/Old/Summon/Hero/3/SummonCard_{01334}_old1.png`）、01335 能量骇客（2026-09-13）、01336 修正者（2026-09-13，「准线」构图：站直的细长能量体横持贯穿整幅画面的亮蓝刻度长杆，灵能组里最亮的一张，luma 169.0）。

**暂无卡图 3 张**：1527 消逝之影、1528 能量收割者、3027 中枢（`summonType: 2`，cost 0，卡图目录见「落盘规范」的兜底条）。

> **「半透明怎么落」这个悬案（2026-09-12 定案）：不做真透明。** 灵能卡一律**实色平涂 + 能量感表现**（边缘发光 / 内部高亮 / 整体提亮），alpha 与其他前缀一样是 1。原先「真机上验半透明透底」的待验证条目随之作废。

### 神灵画卷前缀细化（古卷与命运）

**核心概念：古卷与命运。** 从卷轴 / 典籍 / 星图中来的存在——神秘、古老、预示。不是黑暗，是「命运与智慧」的神秘感。前缀的两条老规矩照旧：① 唯一点缀色 = 幽绿；② 母题取下面这四个。

**统一母题**（神灵画卷前缀只允许这四个）

| 母题 | 说明 |
|---|---|
| 卷轴 / 书 | 古老卷轴、典籍 |
| 星点 | 散布的星辰 / 星图 |
| 符文 | 古卷上的文字 |
| 命运线 | 预示 / 编织命运 |

**配色**

| 项 | 值 |
|---|---|
| 点缀色 | **绿（mystic green）**，全身仍只此一个高饱和色 |
| 主体 | 低饱和灰黑 / 羊皮纸色 |
| 能量 | 幽绿 / 青绿 |

**气质**：神秘、古老、预示——不是黑暗，是「命运与智慧」的神秘感。默认**省掉气质词**，写进角色描述即可。

| 要 | 不要 |
|---|---|
| 卷轴 / 书 / 星点母题 | 无古卷元素 |
| 绿色点缀 | 明亮色 |
| 神秘古老 | 现代 / 科技 |
| 命运预示感 | 纯战斗 |
| 低细节密度 | 堆砌文字纹理 |

> **符文不等于可以在卷面上写满字。** 「古卷上的文字」要抽象成**几枚粗笔画符文**，用平涂色块画，不是密密麻麻的手写小字；缩到卡面尺寸时卷轴本身要能读成一个简单形状。

**负面词**（该后端是否接收 `--negative` 尚未实测；不认就退化成正文里的否定句）

```
modern, sci-fi, bright colors, photorealistic, overly detailed, complex, CG render, 3D, busy background, watermarks
```

**完整提示词（神灵画卷卡）**

```
<基准提示词前缀>
mystic green accent, ancient scrolls and tomes, scattered star motes
<角色描述>
transparent background, 3:4 portrait, no text, no border
```

**数据实况（2026-09-12 核对 `Assets/_Game/Resources/CardData`）**：神灵画卷前缀只有 **3 张**，费用恰好 1 / 3 / 5 各一张，全部 `summonType: 0 (Hero)` 召唤物（既无 ChosenOne、也无 Special，**没有兜底路径的例外**）：

| 卡 | 费用 / 身材 | 卡图目录 | 特性 |
|---|---|---|---|
| 01110 碎片 | 1 费，3/1 | `Hero/1` | 进场：使己方一召唤物退场 |
| 01322 残篇 | 3 费，4/2 | `Hero/3` | 进场：使己方两名召唤物退场，一个回手牌，另一个返还召唤费用 |
| 01523 水墨 | 5 费，1/1 | `Hero/5` | 进场：使己方其他召唤物退场并回到手牌，每退场一个水墨 +1+1 |

**三张都已在 2026-09-12 出图**：01110 碎片 → `Hero/1`、01322 残篇 → `Hero/3`、01523 水墨 → `Hero/5`。三张都是新增卡图，落盘时一并新建了 `.meta`（`Hero/5` 现在有 01514 与 01523 两张图）。逐张画法见 `Assets/_Game/Art/Old/README.md` 的「替换记录」表。

> **01524「画卷之核」不算神灵画卷卡。** 它的 `prefix` 字段是 **`无`**，只是 `traits` 里引用了「己方已存在两名带有神灵画卷前缀的召唤物」这个判定条件。按规则它**不套属性配色**，点缀色由角色描述决定——想让它和 01110 / 01322 / 01523 视觉成组，就得在**角色描述里显式写绿色**，别指望前缀自动带出。
> 另外它的 `baseCost` 是 **0**，卡图目录落在 `Hero/0`。该目录原先不存在（`Hero/` 下只有 1 / 3 / 5，加载器按 `"Hero/" + baseCost` 拼路径，见 `CardDisplay2DNew.cs:646`），已于 2026-09-12 随本次出图新建，`Hero/0/SummonCard_{01524}.png` 已就位。

**形象方向（2026-09-12 讨论；四张均已出图定稿，实际画法见 `Assets/_Game/Art/Old/README.md` 的「替换记录」表）**

| 卡 | 形象 |
|---|---|
| 01110 碎片 | 卷轴小残片 + 绿符文 + 星图 |
| 01322 残篇 | 大段卷轴 + 部分完整的符文 |
| 01523 水墨 | 墨色流动形成卷轴 |
| 01524 画卷之核 | 绿色能量核心 + 环绕符文（点缀色按 `无前缀` 规则显式指定） |

### 生成配方（已实测可用）

- 工具：`imagegen-compat` skill（`scripts/gen_image.py`），`--provider ofoxai`
- 模型：`qwen/qwen-image-3.0-pro`（2026-09-11 起）
  - **不要用 `volcengine/doubao-seedream-*`（豆包）**：出图偏「平面卡通」——粗黑描边、明度只留两级、整体偏亮，缩到卡面尺寸和 01103/01101 并排一看就不是一个画风
  - **不要用 `openai/gpt-image-2`**：它走 Azure 通道，画面里只要出现武器，一律被内容审核拒绝（HTTP 400 safety system）
  - 需要局部改图时走 `POST /v1/images/edits` + 蒙版，用 `google/gemini-3-pro-image`
- `~/.codex/imagegen-compat.json` 里 `providers.ofoxai.model` 已固定为 `qwen/qwen-image-3.0-pro`，`size` 已设为 `1152x1536`，因此 `--refine` 可直接用
- 尺寸：`1152x1536`（3:4 竖版）
- 透明底：**全部费用档**都加 `--transparent` 先生成纯洋红背景再抠图（5 费自 2026-09-25 起同样适用，见「半背景（衬托）画法」条）；衬托物画在画窗中央，四角照旧抠掉
- **人形角色**：提示词正文一律带「脸模提示词片段」（见「1–3 费的脸」节）—— 脸模 = `01504` 的二次元处理，**全档统一**；骨相 / 眼型 / 年龄 / 性别 / 专属标记逐张差异化

#### 局部改图（换脸 / 只改某一处）

```bash
# 1) 打蒙版：白底 + 透明区，透明处 = 要改的地方（OpenAI 约定）
python Tools/imagegen/mkmask.py <原图> <mask.png> "x0,y0,x1,y1" <feather>
# 2) 调接口
python Tools/imagegen/edit_masked.py <原图> <mask.png> <输出> "<提示词>" google/gemini-3-pro-image 1152x1536
```

- **蒙版只是提示，不是硬约束：Gemini 会重绘整张图**。实测给 01105 只蒙头部改脸，结果帽子形状、衣褶、构图全都跟着变了。要做精确保留的改动，这条路不合适
- 只有 `google/gemini-3-pro-image` 这类图像模型能走这个端点。**`volcengine/doubao-seedream-5.0-pro` 不行**，直接返回 `400 endpoint_not_supported`，提示改用 `/v1/chat/completions`
- 返回 **1792x2400、无 alpha**（与 3:4 略有出入），必须重新抠图，再缩放到 `1152x1536`
- **底色不固定：喂进去带 alpha 的 PNG，回来往往是白底而不是洋红底**（01107 修手这次就是纯白 254,254,254）。别照抄上一次的键色，先取四角看一眼。白底要走白键：`key_flood.py <src> <dst> 40 20 130`
- 白底的过渡带**用宽一点**（`20 130`）比窄的（`28 70`）边缘干净：同样的浅色描边点，宽带版几乎看不出来
- 缩放回标准尺寸后要重新居中：`Tools/imagegen/resize_centre.py <src> <dst> 1152 1536`。改图后主体位置通常会变，务必核对 alpha bbox
- 提示词里要显式写「只改渲染方式，不改五官/表情/姿势」，否则模型会顺手换人设

实测 01107 修手：提示词写明「只修右手、其余原样」，出来的整张图与原件**可见差异只有 1.82%**，且全落在手臂/手部，脸和靴子逐像素级一致——所以「重绘整张」不等于「整张都变」，程度取决于提示词

#### 只想改一小块时：整张用 vs 贴回局部

```bash
# 把改图结果的某个区域贴回原图（区域交叉淡入淡出，不是叠加）
python Tools/imagegen/patch_region.py <原图.png> <改图.png> <输出.png> "x0,y0,x1,y1" <feather>
```

- **注意是交叉淡入淡出**：区域外取原图，区域内取改图。若写成「改图叠在原图上」，改图判定为背景的地方是透明的，原图那块**改坏的东西会原样留下**（实测残指没被擦掉）
- 用之前先比两张图的 alpha bbox，位置不一致要先对齐
- **代价是接缝**：两张图的线稿只要差几像素（这次翅膀比原件宽 3px），区域边界扫过翅膀叶脉时会出现**重影**，3 倍放大明显可见。小范围线稿改动、且改动处压在已有图形上时，**直接用整张改图更干净**
- 这次最终就是整张用的（`01107`），`patch_region.py` 留着备用

### 抠图注意事项（踩过的坑）

> **本节对全部费用档适用**（2026-09-25 起 5 费不再例外，见「半背景（衬托）画法」条）。

- **不要用 `--despill`**：洋红键 = 红+蓝，despill 会把画面里的红芒、红布一起洗成灰色
- **不要对洋红底直接用 `--soft-matte` 默认参数**：其内部 `_dominance_alpha` 会把红色通道偏高的像素压成半透明
- **键色必须每张图重新取样**：洋红底本身不稳定，同一批候选图实测出现过 `(238,12,202)`、`(248,28,248)`、`(253,28,248)` 三种。取四角平均色即可，但不要写死
- **过渡带阈值必须按画面调**：主体若本身是紫/红这类与洋红相近的颜色，要收紧阈值。例：01102 的紫袍与键色距离只有 98，用 60/120 会被抠成半透明（alpha 177），收紧到 `<= 35 / >= 75` 才完全实心
- **鲜艳多色卡的 `purge_key` 阈值要单独调**（2026-09-13）：01301「小丑」实测默认 `T=20`（`min(R,B)-G`）会把红色描边的洋红残留一起啃掉、在剪影上留一圈**虚点**，改成 `T=60` 只吃强洋红后边缘干净。另外**被画面内线条围住的洋红，泛洪够不到**（该图右下被台沿线切出一块残片，`key_flood` 报 `protected interior glow pixels` 4000+），只能靠 purge 清掉——所以这类图「直接跳过 purge」同样不对
- **主体本身是大面积紫的卡，`purge_key` 要用 `dist` 模式，别用 `pink`**（2026-09-13）：01317「贪欲之蛇」的深紫（眼睛 / 旋涡 / 能量束）在 `pink` 判据 `min(R,B)-G` 上高达 **48–66**，`pink T=20` 会连主体一起挖掉（实测啃掉 21.6%）；本项目里「紫」是渊前缀的本体色，与洋红键色只差一个通道，`pink` 判据**天然分不开**。改用与键色的 **RGB 距离**（`dist`）就干净了：键色→紫约 141、键色→灰约 208，取 `T=90` 只吃 5938 px 真残留。判断方法：先量主体里最饱和的那个颜色的 `d` 与 `min(R,B)-G`，若两者不构成量级差，就用 `dist`
- **白键（Gemini 改图返回白底）下 `purge_key` 要用 `dist` 且阈值收小**（2026-09-13）：01327「阴影聚合体」加黑雾那次，改图返回 **1792x2400 白底**（254,254,254）——**给 Gemini 喂带 alpha 的 keyed 图，它照样回白底**，别照抄洋红那套参数。白键走 `key_flood 40 20 130`；`purge_key` 若沿用 `T=90`，会把画面里的**浅灰雾气一起当背景啃掉**，实测收到 `T=35` 才只吃边缘残白
- **大面积紫的卡：泛洪会把紫当成背景「啃」出内部空洞**（2026-09-13）：01331「囚牢」的深渊裂缝与门板紫光到洋红键色的距离只有 `46–80`，落在 `key_flood 55 45 95` 的泛洪阈值内，紫被连通着挖掉——`alpha == 0` 的**内部空洞共 3.0 万 px**（`purge_key dist 90` 又补了一刀），在卡面尺寸下就是裂缝里一块透明破洞（透明区在深色卡底上读成灰块）。修法：把 alpha=0 且**不与画布边缘连通**的像素判为「洞」，逐连通块算洞内**原图**像素到键色的中位距离，`> 30` 的判为「被误啃的画」→ 用原图 RGB 回填、alpha 置 255；`<= 30` 的（锁链 / 骨肋之间那类真背景）保持透明。01331 实测回填 13570 px，保留 16370 px 真背景。**同一判据下 01327 的 1.45 万洞像素全是真背景**——所以必须先判后填，不能一律填平。回填后还要再查一遍洋红点（回填会带出键色边缘像素）：用**到键色的真实距离** `d < 45` 定位，**不要用 `min(R,B)-G`**——深紫在这条判据上高达 80，会连主体一起洗掉
- **背景上的投影会被当成「受保护的内部洋红」留下来**（2026-09-17，01339「守望者」）：模型有时会给主体在背景上补一道**投在洋红底上的影子**——一块平涂硬边的暗莓色形状（实测填充约 `(96,48,72)`，正好是 50% 键色混 50% 深描边，四周还带自己的细描边），缩到卡面尺寸是一块扎眼的暗紫斑。**三步收尾全漏**：`key_flood` 的「保护内部近键色像素」规则**故意**把它留成不透明；`defringe` 只在离透明区 `radius` px 的一圈内动手，够不到被主体围住的斑；`clean_pink` 的判据 `min(R,B)-G` 在这块上只有 12–22、低于它默认的 `T=20`。**识别**：先量全图 `alpha>0` 像素的 `min(R,B)-G` 均值（01339 实测 **−6.86**，整张画没有任何合法的洋红），则该值 `> 8` 的实心像素全是键色污染 —— 别只看 `>20` 那一档，投影就落在 12–22 这个夹缝里。**修法（厚块 / 细边分流）**：对 `(alpha>0) & (pink>8)` 求 8 连通块，**3×3 邻域全在掩膜内**（厚实色块）的判为投影、alpha 置 0；**只有 1–2 px 宽**的判为反锯齿边、按 `(d−T0)/(T1−T0)` 重算 alpha 且**只降不升**；两类都先把 RGB 用「最近的非污染像素」（`pink<=0`）回填。01339 实测：投影 1 块 1821 px 置 0、细边 39 px 降 alpha、RGB 回填 2415 px，复检 `pink>8` 只剩 6 px（孤立点）、bbox 未变。**顺序**：这一步排在 `defringe` / `clean_pink` 之后、入位之前

**首选方案：泛洪式抠图 `Tools/imagegen/key_flood.py`（带发光/紫芒的图必须用它）**

```bash
python Tools/imagegen/key_flood.py <src> <dst> <T_fill> <T0> <T1>
# 例：python Tools/imagegen/key_flood.py draft/x.jpg out.png 55 45 95
```

- 原理：只有**与画面边缘连通**的洋红才算背景。画面内部的洋红/紫色（心口发光、能量光丝、紫色描边）被主体隔开、不连通，会被强制置为不透明
- 全局距离过渡带（`d <= T0` 全透、`d >= T1` 全实）只负责边缘抗锯齿；`T_fill` 是泛洪判定阈值
- 为什么非它不可：01103「腐化之心」心口光晕有 **2.5 万+ 像素**与键色距离 < 55，纯全局阈值会把发光抠成半透明甚至抠没
- 泛洪用 numpy 扫描线自实现（Pillow 10 起 `ImageDraw.floodfill` 已被移除，不要再调用）
- 自检：抠完打印「`alpha > 200` 且 `d < 35`」的像素数，必须为 **0**，否则说明有洋红残留
- 自检有盲区：上面那条只查「离键色 `d < 35`」的**实心**残留，查不出深色混色边缘。01126「凝聚体」实测 `d < 35` 为 0、自检全过，但轮廓内外各留一条 1px 暗洋红边（`(121,30,150)` 一类，`d` 约 **112**），共 **2459 px**。蓝描边的卡尤其要当心（蓝与洋红相邻）；不论自检结果，**一律再过一遍 `defringe.py`**，收尾再用 `min(R,B)-G > 30` 数一遍

**必做收尾：去洋红染色 `Tools/imagegen/defringe.py`**

```bash
python Tools/imagegen/defringe.py <cut.png> <clean.png> 3 0.02
```

- 为什么需要：洋红底与主体的抗锯齿像素是**混色**（50% 金 + 50% 洋红 = 粉），而且这些像素的 `alpha` 常常是 **255**，所以只看 alpha 的抠图完全发现不了它们，成品在深底和浅底上都会有一圈粉紫描边
- 做法：保留全部 alpha，只修 RGB。`core` = 收缩 `radius` 像素后的实心区，`rim` = 其余非全透明像素；把 RGB 从 `core` 向外铺进 `rim`；再按混色比例 `t` 过滤——`t <= t_min` 的像素（例如本来就贴在边缘的黑色描边）原样保留
- **不要用反解** `(C - t*K)/(1-t)`：`t` 大时会把 `F` 的误差放大，实测把一条 75% 洋红的边缘像素解成了亮绿色。平涂线稿不需要它，直接把该像素换成 `F` 即可
- 实测效果：01107 的粉紫边缘像素 10,020 → 90；01126 轮廓内外 2459 → 97（`3 0.02`）
- 参数：`defringe.py <src> <dst> [radius] [t_min] [t_abs] [mode]`
  - `t_abs`：绝对兜底。`t` 是相对 `F`（最近的内部色）算的，**当 `F` 本身就是浅色**（浅灰护腕、白色高光、键色是白色）时，边缘的浅色像素算出的 `t` 很小、躲过判定，留成一串浅色「串珠」。给个 `t_abs`（白键用 `80`）就能兜住
  - `mode=all`：无条件替换所有边缘像素。**平涂线稿上实测更糟**——会把手的外轮廓外侧抹成一条深灰带。不要用

**主体内部的洋红杂线：`Tools/imagegen/purge_key.py`**

```bash
python Tools/imagegen/purge_key.py <src.png> <dst.png> 80          # 按到键色的距离
python Tools/imagegen/purge_key.py <src.png> <dst.png> 45 pink     # 按色相
```

- 原因：`key_flood.py` 会**刻意保护**不连通的内部洋红（当作发光），所以生成时留在轮廓**内部**的洋红杂线会活下来
- 只在该图**没有**刻意的紫/洋红发光时才用（01103 那种心口紫芒不能用）
- 褪色的杂线离键色可能很远（实测 d=94），用 `pink` 模式（`min(R,B) - G`）更准：同一条杂线在 `pink` 下得 100，而旁边带紫调的灰发只有 20
- `pink` 模式对**暖粉肤色是安全的**：暖粉是「红高、蓝低」，`min(R,B)` 取到蓝通道所以得分很低（实测 -5）。只有洋红那种「红高蓝也高」才会命中。反倒是 01107 要求的暖调白里透粉肤色，用 `dist` 模式才会误伤
- 顺序：`key_flood.py` → `purge_key.py` → `defringe.py`

### 落盘规范

- `Assets/_Game/Art/Sprites/Generated/` 是**暂存区，不是存档区**：候选图、卡面尺寸对照图、蒙版、`cut` / `clean` / `edit-*` 之类的中间件都只在这里短暂停留，**定稿后一份都不留**（2026-09-16 定，见下面「定稿后一次性收尾」）
- 先写入 `Assets/_Game/Art/Sprites/Generated/`，人工确认后再替换卡图
- 卡图路径由**卡的 `summonType`** 决定（`Assets/_Game/Scripts/Core/CardEnums.cs:2`：`SummonType { Hero, ChosenOne, Special }`），**与 CardData 所在的子目录无关**。对照表：

| summonType | 卡图目录 | 例 |
|---|---|---|
| `Hero`(0) | `Cards/Summon/Hero/{baseCost}`（按**费用**分 1 / 3 / 5，不是阶位） | `.../Hero/1/SummonCard_{01103}.png` |
| `ChosenOne`(1) | `Cards/Summon/ChosenOne`（不分费用） | — |
| `Special`(2) | `Cards/Summon/Special`（不分费用）；**找不到时兜底 `Cards/Summon/Hero/{baseCost}`** | `.../Special/SummonCard_{03001}.png`、`.../Hero/1/SummonCard_{01113}.png` |

- 运行时按这张表拼路径 `Resources.Load<Sprite>`：`Assets/_Game/Scripts/UI/Board/CardDisplay2DNew.cs:643`、`CardDisplay3D.cs:199` 与 `:235`（法术另有一套 `Spell/{Normal|Special}/{cost}`，两个子目录都试）。**放错目录不报错**——静默退回占位图，卡面露出前缀底图 `PrefixArtBG`，只有肉眼能发现

- **Special 卡图可放两处（2026-09-12 起）**：加载器先找 `Summon/Special/`，没有再兜底 `Summon/Hero/{baseCost}`——`CardDisplay2DNew.cs:651`、`CardDisplay3D.cs:208` 与 `:248` 三处同步（少改一处，手牌和棋盘的卡面就会不一致）。两处都有图时以 `Special/` 为准。`01113`「传送阵」（图在 `Hero/1`）、`01306`「阴」与 `01326`「陷阱」（图在 `Hero/3`）即靠这条兜底显示，属既定安排
- **`Hero/0`（2026-09-12 建）**：`CardDisplay2DNew.cs:646` 是 `"Hero/" + template.baseCost` 直接拼字符串，**0 费卡会拼成 `Hero/0`**，而 `Hero/` 原先只有 `1` / `3` / `5`。本次为 `01524`「画卷之核」（baseCost 0）新建了 `Hero/0` 并落图。注意 0 费 `Hero` 型召唤不止它一张：`CardData` 里还有 6 张 `summonType: 0` + `baseCost: 0` 的 tier 0 卡（02002 开心开玩、02004 皇帝的认可、02005 爬！、02006 给你一拳、02009 飘荡元素、02010 背叛），它们同样会去找 `Hero/0`，目前都无图——将来出图就放这个目录
- 2026-09-12 之前只认 `Special/`，那两张卡因此一直显示不出卡图（静默退回前缀底图）。全库 178 张模板按新旧逻辑逐张比对过：**只有这两张的行为发生变化**，其余完全一致
- 替换时**只覆盖 PNG，保留同名 `.meta`**，这样 Unity 里的引用（GUID）不会断
- 卡图 `.meta` 的 `maxTextureSize` 是 **2048**（`enableMipMap: 0`、`alphaIsTransparency: 1`）。出图长边不要超过 2048，否则 Unity 会静默降采样；`1152x1536` 安全

**定稿后一次性收尾：归位 → 清场**（2026-09-16 修：不再留「成品存档」副本）

人工确认定稿之后，不要让它停在临时文件名或 `Generated/` 里，一次把下面几步做完：

1. **归位**：把选中那张放到卡图路径 `SummonCard_{ID}.png` ——
   - 该卡**已有卡图** → 只覆盖 PNG，保留原 `.meta`（GUID 不能变）
   - 该卡**此前没有卡图**（新增） → 必须一并建 `.meta`：复制同组已有卡图的 `.meta`、只改 `guid`；新 GUID 要全库唯一（先扫 `Assets` 下所有 `.meta` 查重）
   - 文件名后缀必须严格是 `SummonCard_{ID}`，运行时按这个路径 `Resources.Load`，写错就加载不到
2. **清场**：候选图、**选中的那张暂存稿本身**、卡面尺寸对照图、蒙版、`cut` / `clean` / `edit-*` 之类的中间件全部删掉 —— `Generated/` 下同 ID **一份都不留**（图已经在 `Resources/Cards/` 那一份里了）
3. 被换下来的旧图归档到 `Assets/_Game/Art/Old/`，并在该目录 `README.md` 的替换记录表补一行（新增卡图也补，注明「此前无卡图」）

> **只留一份（2026-09-16 定）**：定稿后**全库只有卡图一份**。原先的流程会在 `Generated/` 另存一份 `{ID}-{英文短名}-01.png`「成品存档」，而那份与卡图**逐字节相同**，纯属重复 —— 用户点名「归档进库的有两张一样的图」后取消该步。暂存期的文件名沿用 `{ID}-{英文短名}-{候选序号}.png`（例 `01516-swift-shadow-a.png`），**这个名不进库**，只在候选之间区分用。
> **同日已按此清库**：`Generated/` 下与卡图逐字节相同的 **106 对**副本（106 个 PNG + 106 个 `.meta`）已删，106 张卡图一张没动；同日再把剩下 3 个去背前中间件（`01105-veteran-gambler-01-unedited.png`、`01112-sprite-v5-1.png`、`01112-sprite-v5-1-cut.png`）也删掉 —— **`Generated/` 现已清空**（目录本身保留，`Generated.meta` 还在，出图仍写这里）。删除清单存档在会话输出目录的 `deleted_generated_dupes.json`；这些文件原本都被 git 跟踪，误删可 `git checkout -- <路径>` 还原。
> **`Art/Old/README.md` 里 60 多条历史行**仍写着「新图另存 `Art/Sprites/Generated/…`／卡图与归档 PNG SHA256 一致」，那是**旧流程的当时记述**，按原文保留不改；执行时一律以本节为准。

### 路径写法（2026-09-16 定）：给用户看图的路径一律用正斜杠

**这条只管「发给用户看」的输出**（聊天里的 Markdown 图片、链接、路径引用）；仓库内文档里的路径写法照旧，不受本条约束。

- **Markdown 图片 / 链接里的本地路径必须用正斜杠 `/`**：`![候选图](C:/Users/22589/.codex/visualizations/2026/09/16/<会话目录>/01535_b_sheet.png)`
- **绝对不许用反斜杠 `\`**：Markdown 把 `\` 当转义符，`![图](C:\Users\...\x.png)` 会被解析器整段吃掉，用户端只看到一个破图占位符，图根本没发出去 —— 2026-09-16「执行之剑」B 位三候选就是这么发丢的（用户回「图呢」），同一串坏路径重发几次也一样坏，别再重发
- 路径要**绝对、写全**，不要用相对路径或裸文件名
- 正文里**另附一份纯文本路径**（放进行内代码），作为兜底：用户可以直接点开文件
- 图还是显示不出来时，改用 Codex 面板打开（`open_in_codex`，target = file / path），不要再反复重发同一串路径
- 自查：发图前看一眼自己写的那行，**只要出现 `\` 就改掉**

---

## 分辨率 / UI 缩放口径（2026-09-17 定、2026-09-19 改，改 UI 前必读）

### 一、CanvasScaler 全项目统一：1920×1080 + **按宽高比切换 match（宽屏 Match Height / 窄屏 Match Width）**

- `Game.unity` / `Lobby.unity` / `Welcome.unity` 三个主画布**一律** `m_UiScaleMode: 1`、`m_ReferenceResolution: {x: 1920, y: 1080}`；场景里的 `m_MatchWidthOrHeight: 1` 只是**宽屏默认值**，运行时由 `GameSettings.ApplyUiScale()` 按当前宽高比重写（`GameSettings.SafeMatch`）。所以**手改场景里那个 1 没有意义**，要改就改 `SafeMatch`。
- **两种口径**：
  - `宽高比 ≥ 16:9 → match = 1`（Match Height）：画布**高恒为 1080**，宽随宽高比变宽（16:9→1920、21:9→2580）；
  - `宽高比 < 16:9 → match = 0`（Match Width）：画布**宽恒为 1920**，高随宽高比变高（16:10→1200、4:3→1440）。
  - 两种都保证 **1920×1080 设计稿整幅可见**，区别只是多出来的余量给宽还是给高。
- **理由**：3D 与 UI 必须走同一条公式。相机随之切换锁定轴（`SettingsRuntime.FittedFov` / `FittedOrthoSize`，自动挂在所有相机上，无需手动挂）：宽屏锁**垂直** FOV（`像素 / 世界单位 ∝ 屏幕高`），窄屏锁**水平** FOV（`∝ 屏幕宽`）—— 与画布的 `像素 / 画布单位` 同轴，3D 棋盘与 UI 才会在各分辨率下保持相对位置。
- **此前的问题**：`Game.unity` 是 Match 0.5（`∝ √(W·H)`）、`Lobby` / `Welcome` 是 Match 0（`∝ 宽`）—— 三套公式互不相容。16:10 下 UI 与 3D 错开约 **5%**，21:9 下错开约 **16%**，这就是「换个分辨率组件就偏移」的根因。
- **2026-09-19 修的是另一个方向的毛病**：窄屏此前也一律 Match Height，画布宽只有 `1080 × 宽高比`（16:10 → **1728 < 1920**），于是**居中锚定 + 固定宽度**的元素被挤出屏幕。Game 场景实测：右列 `EndTurnButton` / `DrawCardButton` / `ToggleHandButton` / `ConfirmSelectionButton`（`x = 810`、宽 180，右缘 900 > 画布半宽 864）、左列 `Setting`（`x = -813`）在 16:10 全屏下各被切掉约 36 单位，开发者 Game 视图（16:9）却完好 —— 这就是「实际全屏与开发状态显示宽度不一样」的根因。窄屏改 Match Width 后画布宽恒为 1920，这批元素全部回到屏内。
- **仍需守的规矩**：
  - 锚在**上/下边**的元素在任何分辨率下都待在原处（这是想要的）；
  - **居中锚定 + 固定宽度**的元素，只要 `|x| + w/2 ≤ 960` 就永远安全（画布左右各 960 单位在任何宽高比下都在屏内）—— 摆位时按这条量，不要按 16:9 的 960 之外还要靠边量；
  - 全屏铺满的容器仍必须用**拉伸锚**（`anchorMin (0,0)` / `anchorMax (1,1)` + `sizeDelta 0`），不要用「居中 + 固定 1920×1080」。Lobby 的 `StatsPanel` / `GameIntroPanel` / `Scroll View` 已于 2026-09-17 按此改写（16:9 下矩形与改前逐像素相同）。
- **新的画布一律用** `GameSettings.ApplyScalerTo(canvasScaler)`，不要 `AddComponent<CanvasScaler>()` 用默认值（默认是 ConstantPixelSize，会随分辨率改变视觉大小）。
- **World Space 画布**（棋盘槽位）不参与这条，保持世界尺寸。

### 二、贴图导入设置：**缩小必预过滤，放大才用 Point**

判据一句话：**屏幕像素 < 源像素（缩小显示）→ 双线性 + mipmap；屏幕像素 > 源像素（放大显示）→ Point。**

- 卡图源图 1152×1536，实画在 ~90×118 的卡面（缩小 12.8~18 倍）；图标 511×511 画在 ~21px（缩小 24 倍）—— 这批**必须** `filterMode: Bilinear` + `enableMipMap: 1`，否则缩小采样只命中极少数纹素，出现「马赛克 / 抖动」。
- 反之 `Resources/UI` 里的 `OverUI / GetUI / EyeUI / PlayerEnergyUI / PlayerHealthUI / StartUI` 是**被放大**显示的，必须保持 `Point` + 无 mipmap，加了反而糊。
- 卡图还要开 `mipMapsPreserveCoverage: 1`（`alphaTestReferenceValue: 0.5`）—— 卡图走 alpha-test 裁切（`CardCutout.shader` / `CardFaceSprite.shader` 的 `clip(a - cutoff)`），不保留 alpha 覆盖的话缩小后卡牌边缘会「化开」。
- **自动执行**：`Assets/_Game/Editor/TextureImportSettingsGuard.cs` 的 `AssetPostprocessor` 会对新导入的贴图自动套用；菜单 `Tools/设置/贴图导入设置体检 & 修复` 可随时重跑（幂等）；`Tools/设置/贴图导入设置体检报告（只读）` 只报告不改。
- 历史对照：归档在 `Assets/_Game/Art/Old/` 的旧图当年就是双线性 + mipmap，所以它们在缩略图里反而比新图清楚 —— 这就是判断依据的来源。

### 三、字体：**绝对不要**给字体图集加 mipmap（2026-09-17 踩过并已回滚）

- 项目 7 个中文字体（`Assets/_Game/Fonts/NotoSerifCJKsc-* SDF.asset`）的 `m_AtlasRenderMode` = **4165 = `SDFAA`**，**本来就是真正的 SDF**；`LiberationSans SDF` 的 4169 = `SDFAA_HINTED`，两者只差 hinting，**不存在「位图 vs SDF」的画风差异**，别再按「重烘成 SDF」去改。
- **图集 `m_MipCount: 1` 是正常的、必须保持。** 别把「贴图缩小时要预过滤」那条规律套到字体上 —— 2026-09-17 就这么错过一次：给 7 个图集加了 13 级 mip 链，结果**整片 UI 文字变软**，当天已 `git restore Assets/_Game/Fonts` 回滚，并删掉了那个工具（`TMPFontAtlasMipFixer.cs`，连自动运行一起删，免得再犯）。
- **为什么反着来才对**：TMP 的 SDF shader 假设采样到的是**未被平均过的**距离场，靠 `_GradientScale`（= padding + 1）把灰度还原成边缘。mip 是对距离场做盒式平均 —— 梯度斜率被压平、边缘过渡被拉宽 → 文字变软。Unity 自带的 `LiberationSans SDF` 与 TMP 生成器都产出 `m_MipCount: 1`，那是设计，不是疏忽。
- 量化（Lobby 实测）：图集按 36pt 烘焙，Lobby 主力字号 24pt（23 处）。**文字在屏上占几个设备像素 = 设计字号 × 画布倍率**，而画布倍率 = 屏幕高 / (1080 / 界面缩放) —— 编辑器 Game view 只有 992×558 时倍率是 **0.52**，24pt 正文落到 **12px**，笔画被抗锯齿吃掉 —— 这才是「字体发糊」的真因（**不是** mip，也**不是** filterMode）。实测过程见下面「五、清晰度（像素预算）」。
- **「把图集点号从 36 降到 24」这条 2026-09-17 作废**（当时是按「缩小比靠近 1」推的，方向错了）：图集点号定的是 **SDF 距离场的细节上限**，采样点号应 ≈ 文字可能出现的**最大**像素高；往下调只会让大字号先坏。当前 1080 基准 + 图集 36 正好对齐 2560×1600 原生全屏（24pt × 1.48 = 35.6px ≈ 36）—— **不要动**。真要重烘图集，必须保持 GUID 不变，否则预制体引用会断。
- 另：`TMP Settings.asset` 的 `m_fallbackFontAssets` 已补上 `NotoSerifCJKsc-Regular SDF`（此前是空数组，缺字会直接掉到没有中文的 LiberationSans）—— 这条与 mipmap 无关，保留有效。
- **新增 UI 文案前先查缺字**：字体图集是按 `Assets/_Game/Fonts/GameCharacters.txt`（4275 字）**静态**烘焙的，**不在这个集合里的字会渲染成空白 / 豆腐块**。已知缺 `×`(U+00D7)、`÷`、`²`、`≥`、`∝`、`→` —— 分辨率文案因此统一写 ASCII `x`（`1920x1080`），关闭按钮写 `X`。加新文案前把字往 `GameCharacters.txt` 里补并重烘。

### 四、设置面板（2026-09-17 新增）

- `Assets/_Game/Scripts/Settings/`：`GameSettings.cs`（PlayerPrefs 持久化 + 应用）、`SettingsPanel.cs`（运行时构建的设置 UI）、`SettingsRuntime.cs`（启动应用 + F10 热键 + 屏幕尺寸监听）、`SettingsLauncher.cs`（自动接线场景里名为 `Setting` 的按钮）。
- **入口不需要改场景**：`SettingsLauncher` 每次加载场景都扫一遍，名为 `Setting` 且没有 `SettingsButton` 的按钮 → 直接开关设置面板（Lobby 的预留按钮走这条）；已有 `SettingsButton` 的（Game 场景的投降面板）→ 往它的面板底部注入一个「游戏设置」按钮。
- **画质 / 帧率 / 垂直同步 / 窗口模式 / 音量 / 界面缩放一律走 `GameSettings`**，`GPUOptimizer` 只负责「不进设置面板的渲染开关」（MSAA=2、各向异性、相机 HDR）。别再把 `SetQualityLevel(5)` / `vSyncCount` / `targetFrameRate` 硬编码回去 —— 那会在每次加载场景时覆盖玩家设置。
- 顺序要求：`SetQualityLevel` 会重写 `vSyncCount` / `targetFrameRate` / `antiAliasing` / `anisotropicFiltering`，所以这几项**必须放在它之后**设（已封在 `GameSettings.ApplyQuality()` / `ApplyFrameRate` 里）。

- **配色：白底（浅色）主题**（2026-09-17 定）：`SettingsPanel` 的 `ColPanel` = 纯白，靠**灰阶差**分层而不是边框（`ColBox` #F3F3F5 / `ColBtn` #E5E5E9），文字近黑、次级文字中灰。两条容易踩的：
  - 按钮的 hover / pressed **一律往暗里走**（`NewButton` 里乘 0.94 / 0.86）。深色主题那套「乘 1.45 提亮」在白底上会顶到纯白 —— 按钮一悬停就消失。
  - 遮罩 `ColBackdrop`（黑 72%）**保持深色**：大厅背景本身接近纯白，没有深色压底就看不出面板边界。
  - 注入到 Game 场景投降面板的入口按钮单独用 `ColHostBtn` / `ColHostTxt`（宿主面板是深色的），别跟着白底主题走。

### 五、清晰度（像素预算）：改分辨率 / 字号 / 缩放前必读（2026-09-17 定）

一句话：**清晰度 = 设备像素够不够用。** 文字与图案是两条独立预算，各有下限，且**互不补偿**（把字放大救不了被拉大的位图，反之亦然）。

| 对象 | 屏上像素 | 下限 | 上限 |
|---|---|---|---|
| 文字（TMP SDF） | 设计字号 × 画布倍率 | **≥ 24px**（汉字笔画才站得住） | **≤ 图集点号 36**（再大距离场细节不够，边缘发虚） |
| 图案（位图） | 设计尺寸 × 画布倍率 | —— | **≤ 源像素**（超过就是放大，必糊；缩小倒是没问题，靠 mipmap + 双线性） |

- **画布倍率 = 屏幕基准边 / (基准长度 / 界面缩放)**：宽屏用屏幕高（基准 1080），窄屏用屏幕宽（基准 1920）—— 见 `GameSettings.ScreenScale`。倍率 1.0 = 与设计稿 1:1。
  - `GameSettings.RenderScale` 就是这个值；`GameSettings.DescribePixelBudget()` 把「屏上像素 + 档位」拼成一行 —— 启动时打一次 Console，设置面板「界面」栏也常驻显示（**改完设置先看这行落在哪一档**）。
- **编辑器 Game view 的实际渲染分辨率 = 视图的逻辑尺寸（点），不是物理像素。** 2026-09-17 实测（本机 2560×1600 / 150% 显示缩放）：`UserSettings/Layouts/default-2022.dwlt` 里 Game view 的 `m_Pos` = 992×657 点、`m_TargetSize` = **992×558**、`m_ZoomArea.m_Scale` = 1.5（= 该屏的 `pixelsPerPoint`）；用户截图里游戏区实测 1475×823 物理像素 ≈ 992×558 × 1.5 —— 即游戏只渲染 **992×558**，再被 **1.5×** 放大到屏幕上，倍率 **0.52**、24pt 正文只有 **12px**，于是「怎么调都糊」。
  - 所以**这不是项目设置的问题**：同一份工程构建后按原生分辨率跑，倍率 1.48、24pt = 35.6px，已经在最佳档。
  - 预览要看清：把 Game view 拉大 / 勾 `Maximize on Play` / 分辨率选 `Fixed Resolution 1920×1080`。
- **编辑器预览糊的根因（2026-09-17 逐像素实测确认）：Game view 工具栏里 Aspect 下拉旁那个方块按钮 `Low Resolution Aspect Ratios` 开着。** 它开着时 Unity **忽略 DPI**，把渲染分辨率压到视图的「逻辑尺寸」而不是物理尺寸 —— 即 **992×558**（逻辑）而不是 **1488×837**（物理），再被 **1.5×** 放大铺满屏幕。Scene 视图没有这道中间缓冲（UI 直接按最终像素栅格化），所以同一个界面在 Scene 视图里清晰、在 Game 视图里发虚。
  - **一眼判据**：Game 视图 `Scale` 滑条的**最小值 = `pixelsPerPoint`**（本机 150% 显示缩放 → 最低就是 `1.5x`）就是这个开关开着的标志 —— 最低档已经在「放大 1.5 倍」了。
  - **量化**（同一屏、同一处文字，两张截图逐像素测）：Game 视图「随机匹配」外框 72×16px、暗像素平均亮度 **144**；Scene 视图 73×17px、平均亮度 **106**。**尺寸几乎一样、笔画却浅一大截 = 被重采样糊过**（纯放大，不是字变小）。标题更明显：Game **110** / Scene **46**。
  - **修法**：关掉那个方块按钮 → 渲染分辨率 992×558 → 1488×837（像素量 ×2.25），立刻回到 Scene 视图的清晰度；想更狠再选 `Fixed Resolution 1920×1080` 或勾 `Maximize on Play`。
  - **一键验证**：Game 视图工具栏 `Stats` 面板里的 `Screen: W x H`，以及启动后 Console 那行 `[清晰度]` —— 现在应报 992x558，改完应报 1488x837 或 1920x1080。
  - **已按此处理（2026-09-17）**：`UserSettings/Layouts/{default-2022,CurrentMaximizeLayout}.dwlt` 里的 `m_LowResolutionForAspectRatios` 已整条清零（改前是 `01 00 00 …`，只有第 0 档是开的）。同一状态也可以在 Unity 里用菜单读写：`Tools/设置/Game 视图清晰度诊断（只读）` / `Tools/设置/Game 视图：关闭低分辨率渲染`（`Assets/_Game/Editor/GameViewClarityCheck.cs`，用 `SerializedObject` 直接读写那个字段，不需要反射）。
  - **`.dwlt` 是用户偏好、不入库**（`UserSettings/` 不在版本控制里），换机器 / 换工程不会带走 —— 所以别只靠手改文件，新环境先点一次那个诊断菜单。
- **界面缩放（`UiScale`）是唯一安全的旋钮**：范围 0.5~1.5，只改「参考分辨率 = 1920/UiScale」。调大 → 字更大更清楚、位图被拉大；调小 → 两边一起变差（**所以别把 UI 往小调来「腾地方」**）。
- **不要为了「字更清楚」去降设计分辨率或调大字号**：24pt 正文在 1600p 原生全屏已贴住图集 36；再往上（4K 全屏 = 48px）反而超过图集先坏。要动就必须同时重烘图集并同步 `GameSettings.TextAtlasPointSize`。
- **全屏一律跟随桌面原生分辨率**（`ApplyDisplay` 里写死，面板在非窗口化时禁用分辨率档）：让玩家用非原生分辨率顶全屏是「系统先渲染再缩放」，比窗口化还糊。

---

## 光环 / 持续效果的归属判定（2026-09-17 定，写效果前必读）

一句话：**「己方」效果只作用于来源所在的那半边场，而且 owner 必须显式传进来，不许在函数里写死 `6..11` 或扫全场 `0..11`。**

- 半场约定（`BoardManager`）：**0-5 = 对手 / AI，6-11 = 本机**；`IsAllySide(slotID)`、`GetSideRange(slotID, out s, out e)`、`GetOwnerPlayer(slotID)`、`GetEnemySideRange` 就是给这件事用的。跨端时服务端把远端玩家的「自己的半场」镜像到 0-5，所以 `this == NetworkPlayer.Local ? 6 : 0` 这套换算在服务端也成立。
- 光环查询：用 `GlobalEventManager.IsAuraActiveOwnedBy<T>(ownerIsHost)`（商人 01520 / 能量收割者 01528 已改用它）。**不要**再写「遍历 `GetAllAuras()` 找到任意一个 `MerchantAura` 就返回 true」——那是 side-agnostic，AI 放在 0-5 的商人会减玩家手牌的费。
- 前缀光环：中枢(03027)的灵能前缀**只有一个结算点** —— `HandManager.ApplyCorePsiAura(NetworkPlayer owner)`（场上全体 + 手牌召唤物，幂等）。`Player.ApplyCorePrefix` / `NetworkPlayer.ApplyCorePrefix` 都是薄包装，`HandManager.ProcessAuras` 里按 `BoardManager.GetOwnerPlayer(slot.slotID)` 调它；AI 那侧另在 `SimpleAI.TryPlayOneCard`（AI 新召唤物进场）与 `NetworkPlayer.AddServerSideCard`（AI 抽牌）补调。
- `HandManager.IsSuppressorOnField(sideSlotID)`：缄默神官(03501)的阶位+1 只认「进场者自己半场」的神官（**旧写法写死 6-11，AI 的英雄进场会白吃玩家神官的 +1**）。

### 离线模式已修的错位 bug（2026-09-17，症状：AI 放的牌作用到玩家身上）

| 症状 | 根因（改前） | 修法 |
|---|---|---|
| AI 放的中枢给**玩家手牌**加灵能前缀 | `NetworkPlayer.ApplyCorePrefix` 扫全场 `0..11` 找 03027；`HandManager.ProcessAuras` 的手牌段写死 `NetworkPlayer.Local.handCards` | 两者统一改走 `HandManager.ApplyCorePsiAura(owner)`，owner 由落点/抽牌者解析 |
| AI 放的中枢给**玩家手牌**加灵能前缀（第二条入口） | `HandManager.SummonCoreEffect` 的 AI 分支里 `PlaceCardToSlot` → `ProcessAuras` 又对 `Local.handCards` 跑了一遍 | 同上；AI 分支改为显式 `ApplyCorePsiAura(NetworkPlayer.Remote)` |
| AI 的英雄进场吃到**玩家**缄默神官的阶位+1 | `IsSuppressorOnField()` 写死 `6..11` | 加 `sideSlotID` 参数，`ProcessAuras` 传 `slot.slotID` |
| AI 的商人/收割者减**玩家**手牌费 | `IsMerchantOnField` / `IsEnergyReaperOnField` side-agnostic | 统一改走 `IsAuraActiveOwnedBy<T>(this == Local)` |
| AI 的皇帝认可(02004)给**玩家**的牌加「渊」前缀、还让玩家白摸 1 张 | `CardDrag.EmperorsApprovalEffectCoroutine` 借 `BuildHandPlusFieldCardList`（本机手牌 + 6-11 场）取候选，`CardDisplayPanel.ShowWithCallback` 在 `IsAIEvaluating` 下自动确认第一张 | 加 `casterIsHost` 参数；AI 分支走 `HandManager.PickAIOwnSummon` + `CommitAIOwnCardPrefix`，只碰 AI 自己 |
| AI 的伟大进化(02203)给**玩家**的召唤物阶位+3 | `HandManager.GreatEvolutionEffect` 写死 `6..11` + `NetworkPlayer.Local.handCards`，无 AI 分支 | 加 `casterIsHost` 参数；AI 分支走 `PickAIOwnSummon` + `CommitAIOwnCardTier` |
| AI 的聚光灯(02310)被**玩家**场上没有空格挡掉（AI 自己有空位也放不出） | 门控写死 `6..11` | 门控改按「施法者己方半场」（AI → 0-5） |
| AI 的背叛(02010)被**AI 自己**场上没有空格挡掉，却把叛徒放到 6-11 | 门控写死 `0..5`，与落点不同侧 | 门控改按「叛徒要落进去的那半场」（AI → 6-11） |
| AI 中枢给**自己手牌**加的灵能前缀放牌时丢了 | AI 手牌是 server-only 追踪对象，`GivePrefix` 改的是 `ServerPlayCard` 会重建的那个 `CardInstance`；而放牌时真正生效的是 `_handPrefixOverrides` / `_handTierOverrides` | 新增 `NetworkPlayer.ServerSetHandCardPrefixOverride` / `ServerSetHandCardTierOverride`（非 Command，服务器权威侧直接登记），`ApplyCorePsiAura` 的 AI 分支改用它 |
| AI 在没有己方召唤物时空放 02004 / 02203（白扣费） | `SimpleAI.TryFindSpell` 不检查目标存在性 | 加 `SimpleAI.HasOwnSummonForSelfBuff()` 守卫，一个目标都没有就留手 |

**判据（改效果时自查）**：写完问一句 —— 「这张牌是对手放的，它会不会作用到我这半边？」只要函数里出现 `NetworkPlayer.Local`、写死的 `6..11` 或 `0..11`，就大概率是错的。`Player` 组件是本机单例（6-11），`NetworkPlayer.Remote` 是 AI/远端（0-5）。

### 雾隐(01517) 隐藏态：刷新入口是 `BoardSyncManager.RefreshMistHiderHiding()`（2026-09-21 定，改隐藏/沉默类效果前必读）

**隐藏是实时谓词，不是一次性动作。** 「对方半场是否该盖住」由 `GlobalEventManager.IsMistHiderActiveOwnedBy(false)` 现算（本端 `0-5` = 对手半场，Host 与纯客户端同一口径），而它同时受三件事影响：① 雾隐源进出场；② 己方能量骇客(01335)对位封锁 / 骇客退场 / 骇客被沉默；③ 缄默神官(03501)阶段沉默生效或到期。**②③ 都不会让 `MistHiderAura._isActive` 翻转**（那只跟 `source != null` 走）。

- 统一入口：`BoardSyncManager.RefreshMistHiderHiding()` —— 按实时谓词重算 + Host 侧立即套用到本端 `0-5`（槽位卡**与宿主在该半场的附着牌**一起切）+ `MarkDirty()` 让对端重算。新增/修改任何会改变「雾隐是否生效」的效果，**必须显式调它**（`BoardSlot.SyncMistHiderDisplay()` 是旧名别名，直接调也等价）。
- 旧写法（已废）：`BoardSlot.SyncMistHiderDisplay()` 遍历光环调 `MistHiderAura.IsActive()`；该方法只在 `_isActive` 翻转时才标脏 → ②③ 下是纯空操作，隐藏态只能等下一次无关的板面同步才翻转（症状：骇客封禁后对方卡仍隐藏、封禁解除后不恢复、雾隐生效时附着牌不跟随）。
- 附着牌：`SetHalfHidden` 按 `hostSlotID` 判归属，所以只有被隐藏那半场的附着牌会盖住。**新落地的附着牌要当场取终态**（`hostSlotID <= 5 && Card3DHover.EnemyCardsAreHidden` → `SetHidden(model, true, true)`），否则会先露一帧正面。已覆盖：`HandManager.PlaceAttachedCard`、`NetworkPlayer.ApplyAttachDiff` / `CmdReportMyBoard`、`BoardSlot.ApplyFairyReattachToSlot`。
- 客户端方向：纯客户端（`NetworkServer.active == false`）不本地决定隐藏，`0-5` 由服务端同步头字段（`hostMistHider`）在 `ApplySync` 里驱动；`RefreshMistHiderHiding()` 在客户端只标脏、不动本端显示。
- **隐藏归雾隐源，不归被隐藏的卡（2026-09-21 定）**：翻回正面**只有一个条件** —— **01517 自己被完全沉默**（`IsMistHiderActiveOwnedBy` → `IsFullySilenced(雾隐源)`）。01335 放在**被隐藏的卡**对位，只沉默那张卡自己的特性，**不动隐藏** —— 隐藏不是那张卡自己的属性，与「格子给予的效果 / 光环给的状态」同族，都不归它管。反之只要雾隐源 `IsFullySilenced` 为真就翻回正面（对位骇客 **或** 03501 阶段沉默都算），封锁解除（骇客退场/被沉默、阶段沉默到期）谓词自动回 `true`，隐藏随之恢复。
- **附着状态的 01517 没有自己的槽位**：「对位」由 `GetSlotOf` 解析到宿主槽（`hostSlotID`），即骇客要落在**宿主槽的对位**才封锁得住雾隐；01335 自己附着时同理（按宿主槽去封锁对位），`IsSlotHackedByEnergyHacker` 两条都覆盖。不要另造一套「按视觉坐标判对位」的写法。

### `RunAsLocal` 与协程：`NetworkPlayer.Local` 只在同步段可信（2026-09-17 定，改法术 handler 前必读）

`NetworkPlayer.RunAsLocal(action)` 会在 `action()` 执行期间把 `Local` 换成**施法者**、`Remote` 换成施法者的对手，`finally` 里还原。三条法术分发路径（`NetworkPlayer.CmdResolveSpell`、`SimpleAI.PlaySpell`、`CardDrag.ResolveSpellEffect`）都包在它里面。

- **同步 handler**（`SpellHandlers` 里绝大多数）：`NetworkPlayer.Local` = 施法者、`NetworkPlayer.Remote` = 对手 —— **写 `Local` 是对的**，`02001/02002/02006/02009/02104/02105/02110/02205/02209/02301/02308/02309/02404/02407/02508/02005` 都属这一类，已逐条核对无需改。
- **协程**：`StartCoroutine` 会**立即**执行协程体到第一个 `yield` 为止，所以「第一个 `yield` 之前」的代码仍在 `RunAsLocal` 里（此时 `Local` = 施法者）；**第一个 `yield` 之后的代码跑在 `RunAsLocal` 之外，那一刻 `Local` 已经还原成人类**。这就是 02004 出 bug 的原因（它的第一句就是 `yield return null`）。

**因此**：协程里的 AI 分支判据用 `SimpleAI.IsAIMatch && !casterIsHost`（`casterIsHost` 从 `ctx.spellCasterIsHost` 传入，与时机无关）；只靠 `SimpleAI.IsAIEvaluating` 的判断仅对「第一个 yield 之前」成立 —— 现存几处（02106 / 02212 / 02310 / 02403 / 02408 / 02501 / 02302）都恰好在协程体开头，故仍有效，但**新写协程不要沿用这个写法**。

**AI 的「己方」工具**（`HandManager`，2026-09-17 新增，AI 分支请复用）：`PickAIOwnSummon(filter)` 在 AI 场上 0-5 → AI 手牌里挑费用最高的合格召唤物；`CommitAIOwnCardPrefix(ci, prefix)` / `CommitAIOwnCardTier(ci)` 把改动落到权威侧（场上 → `UpdateValues` + 板面同步；手牌 → 登记覆盖，放牌时注入）。

### 按半场解析玩家：用 `LocalHalfPlayer` / `RemoteHalfPlayer`，不要用裸 `Local` / `Remote`（2026-09-17 定）

`RunAsLocal` 换的是**静态引用**，**棋盘槽位不会跟着换**：0-5 永远是 AI/对手的物理半场，6-11 永远是本机半场。所以「按对象身份反推半场」的写法在 `RunAsLocal` 期间会整体反掉 —— 这是本次一整类错位 bug 的总根因。

| 取法 | 含义 | 用在哪 |
|---|---|---|
| `NetworkPlayer.Local` / `Remote` | **当前施法者 / 其对手**（`RunAsLocal` 内会换） | 只在法术 handler 的同步段、以及进入 `RunAsLocal` **之前**的快照里用 |
| `NetworkPlayer.LocalHalfPlayer` / `RemoteHalfPlayer` | **6-11 / 0-5 半场的固定归属者** | 想表达「我这半边 / 对侧半边」时一律用它；非 `RunAsLocal` 期间与 `Local`/`Remote` **完全相同**（零风险替换） |
| `NetworkPlayer.HalfOwner(slotID)` | 按槽位解析归属玩家 | `BoardManager.GetOwnerPlayer` 的底座（已改为走它） |

本次按这条口径替换过的位置（都是「半场归属」语义，替换后在非 `RunAsLocal` 期间行为一字不变）：`Counters/CounterManager.cs`（victim / beneficiary）、`Effects/Handlers/DeathHandlers.cs`（owner）、`Battle/BattleManager.cs`（revOwner / owner）、`Board/BoardSlot.cs`（targetOwner / owner / oppNp，共 6 处）、`Network/NetworkPlayer.cs`（`ServerPlayCard` 的 `this != Local`、registry 的 `this == Local ? 0 : 1`、`IsMyTurnOnServer`、`IsMerchantOnField` / `IsEnergyReaperOnField`、oppNp 共 4 处）、`Turn/TurnManagerNetwork.cs`、`Network/BoardSyncManager.cs`、`Manager/GlobalDeathEventHandler.cs`、`UI/Hand/HandManager.cs`（守望者 01339）、`Player.cs`（`ApplyCorePrefix`）。另外 `HandManager.SummonCoreEffect` 的 AI 分支改走 `RemoteHalfPlayer`（原来写 `Remote`，在 `RunAsLocal` 里其实拿到的是玩家）。

**已知非问题（仅日志噪音）**：`HandManager.SummonCoreEffect` / `SummonSmallEvilEffect` / `BetrayalEffect` / `DoorEffect` 的 AI 分支里那句 `if (NetworkClient.isConnected) NetworkPlayer.Local?.CmdPlayCard(...)` —— 该分支只在离线 AI 对局跑，此刻 `Local` 是 AI 对象、`isOwned == false`，Mirror 打一条 "called on ... without authority" 警告后直接 return（不改变任何状态）；板面模型已由 `PlaceCardToSlot` 建好、`MarkDirty` 负责同步。留着不影响结果，故未改。

**未覆盖**：附着物（`BoardManager.attachedModels`）不参与 03027 的中枢光环，只扫 12 个槽位（与原实现一致）。
## 进场窗口与死亡判定（2026-09-17 定，改 `StartOnEnterEffect` / 死亡扫描前必读）

**症状**：召唤物生命值已经 ≤0，却一直留在场上不退场（例：佣兵 01104、逃兵 01312）。

**根因**：死亡扫描 `BoardSlot.CheckAndHandleDeaths` 会跳过 `_enterEffectRunning == true` 的卡（"进场效果还没结算完，先别判死"，守卫见 `EnterWindowStillActive`）。这个标记一旦被永久留在 `true`，该卡就**永久免疫死亡**。本次堵掉的三条泄漏路径：

| 泄漏路径 | 说明 | 修法 |
|---|---|---|
| 卡面 `hasOnEnter: 1` 却没有 Enter handler | `EffectDispatcher.Dispatch` 返回 false → 走「未注册卡回退」分支；那句 `CleanupAfterPlacement()` 跑在 `NestingContext.Depth == 1` 里被 `!IsNested` 门挡下，`Exit()` 之后又没人再清 → 标记永久为 true。**实测受害者：01312 逃兵（3 费 5 血墙）** | ① `01312.asset` 的 `hasOnEnter` 改 0（它的效果是先手，`hasFirstStrike` 已有）；② 标记清除统一改走 `inst.EndEnterEffect()` 并放进 `StartOnEnterEffect` 的 `finally` |
| 协程挂死 | 进场 handler 卡在一个再也不会到来的选择回调上（`WaitUntil(() => done)`）→ `StartOnEnterEffect` 永不收尾 | ① 选择被强退时把 `null` 喂回等待者（`SelectionManager.CancelPendingSelections`，挂在 `ForceEndAll` / 清栈上）；② AI 半场的进场选择一律自动裁决（`BoardSlot.enterEffectActorIsAI`） |
| 任何未知路径 | —— | 死亡扫描兜底：`EnterWindowStillActive()` 判定标记「残留」（`NestingContext.Depth == 0`）或「悬挂」（年龄 > `CardInstance.EnterEffectHangSeconds` = 10s）时强制解除，并打一条 `[DeathScan] 进场窗口残留/悬挂，强制解除` 警告 |

**口径（改这块代码时必须守）**：

1. `_enterEffectRunning` **只**由 `CardInstance.BeginEnterEffect()` / `EndEnterEffect()` 维护，是深度计数 `_enterEffectDepth` 的派生量；两者必须成对，`End` 必须放在 `finally` 里。**不要再直接给 `_enterEffectRunning` 赋值**（旧代码的 `inst._enterEffectRunning = false` 已全部替换）。
2. 深度归零的那一刻若 `currentHealth <= 0` → `EndEnterEffect` 内部补一次死亡扫描，兑现「窗口结束之后再判死」。
3. 「`NestingContext.Depth == 0`」与「某张卡 `_enterEffectRunning == true`」**不可能同时成立**（唯一的置位点在 `NestingContext.Enter` 之后、同一个 `try` 里），所以深度为 0 时还自称"进场中"的标记必为残留 —— 这是兜底判据的地基，别绕开 `StartOnEnterEffect` 去开进场窗口。
4. 进场效果里的目标选择不能只靠 `SimpleAI.IsAIEvaluating` 判断「是不是 AI 在选」：AI 的卡会在非评估期进场（战斗阶段被召唤 / 学者复制的进场 / 人类回合里被效果召唤）。用 `SimpleAI.IsAISide(slotID)`（或 `BoardSlot.enterEffectActorIsAI`）。
5. 给新卡加进场效果：卡面 `hasOnEnter: 1` **必须**配一个 `EnterHandlers.Register("<tid>", Trigger.Enter, ...)`；漏配时 Console 会打 `[StartOnEnterEffect] 进场效果未注册` 的 **Error**（卡面无效果 + 触发标志不一致，是数据错误不是正常回退）。
6. 选项被强退（超时 / 强制收尾）时，`SelectionManager.BeginSelection` 登记的回调会以 `null` 被调一次 —— handler 的回调必须容忍 `targetSlot == null`（这也是 `BeginSelection` 原有约定："选不到合法目标传 null 结束选择"）。
## 回手 / 进手牌：实例状态继承规则（2026-09-17 定，改退场回手 / 抽牌进手牌前必读）

**症状**：AI 的执念亡魂(03504)退场回手后像换了一张新牌 —— 「退场回手费-1」不累计，场上吃到的永久数值改动也不生效。

**根因**：回手按 owner 分流（`DeathPipeline.ExecuteCommon` → `NetworkPlayer.AddCardToHandFromInstance`），人类侧走「Instantiate 预制体 + `CopyFrom(oldInstance)` + 一段『回手即清』规则」，AI(server-only) 侧走 `NetworkPlayer.AddServerSideCard`，而那里**只按模板 `InitFromTemplate`** —— 旧实例的费用 / 永久数值 / 前缀 / 授予特性 / 状态全部丢弃，等于新建一张牌。两条路径口径分叉就是根因。

**口径（只此一处，别在分支里各写一份）**：

| 类别 | 字段 | 回手后 |
|---|---|---|
| 永久改动（保留） | `currentCost` / `costReduction` —— 03504 的「退场回手费-1」靠它累计（`Handle03504` 在**退场时**先 `costReduction++`、`currentCost--`，再回手） | 原样带进手牌 |
| 永久改动（保留） | `baseAttack` / `baseHealth` / `baseMaxHealth`、`prefixes`、`grantedTraits`、`activeStatuses`、`totalDamageTaken` | 原样带进手牌 |
| 场上临时状态（清除） | `tempAttackBoost` / `tempHealthBoost`、`buffedBySage` / `buffedByEmperor`、护盾、01336 附着授予特性、01520 / 01528 来源状态、`handledReturnToHand` | 清零 / 移除 |
| current* 归位 | `currentAttack = max(0, baseAttack)`、`currentHealth` / `currentMaxHealth` 同理、`currentTier = baseTier` | 「回手即清」只清场上临时加成，**永久改动一律走 `base*` 保留** |
| 回手后重判 | `merchantDiscounted` / `energyReaperDiscounted`（商人 01520 / 收割者 01528 是否在**该手牌主人的半场**）；01524 画卷之核 `currentCost = 0` | 按光环现状重算 |

实现落在 `NetworkPlayer.ApplyReturnToHandRules(inst, template)`：人类路径 `AddCardToHandFromInstance` 与 AI 路径 `AddServerSideCard(data, iid, oldInstance)` **唯一共用点**。AI 侧若旧实例是影子类（`isShadow`）则不进手牌（与人类分支同款 `Destroy` + `return`）。

**通道表（状态怎么进板面）**：

| 侧 | 手牌 → 板面 | 说明 |
|---|---|---|
| 人类 | `HandManager.PlaceCardToSlot` 里 `cardInst.CopyFrom(sourceInstance)` | 整份实例随拖放进板 |
| AI(server-only) | `ServerPlayCard` 重建 `CardInstance` 时按 instanceID 注入覆盖表：`_handTierOverrides` / `_handPrefixOverrides` / `_handBaseOverrides` | 前两张早就有（02203 伟大进化 / 02004 皇帝认可）；`_handBaseOverrides` 为本次新增 —— 回手时由 `AddServerSideCard` 登记 `base*`，否则永久数值改动到下一次放牌就被 `InitFromTemplate` 抹掉 |

**新增卡 / 新效果时自查**：任何「让一张牌离开板面再回到手牌」的路径，都必须把**旧 `CardInstance` 传下去**（AI 侧是 `AddServerSideCard` 的第三个参数）。只传 `templateID + instanceID` 的写法一律会退化成「全新个体」。

**已知偏差（记录未改）**：AI 的 `AddServerSideCard` 不查 `maxHandSize`（人类侧 `AddCardToHandFromInstance` 是 `handCards.Count >= maxSize → return`，手牌满时该牌消失）；这条差异在回手路径上一直存在，本次不动。
### 回手类卡片覆盖清单（2026-09-17 盘点）

**入口只有两个**（守住这两处即覆盖全部回手类卡）：`NetworkPlayer.AddCardToHandFromInstance`（人类 / 主机，含 UI 路径）与 `NetworkPlayer.AddServerSideCard(..., oldInstance)`（AI，server-only）。效果牌统一走 `NetworkPlayer.ReturnCardToOwner`，它只负责按 owner 分流到上面两个。

| 卡 | 回手实现位置 | AI 侧 |
|---|---|---|
| 03504 执念亡魂（神选者，数据在 `ChosenOneData/03504.asset`）| `DeathPipeline.ExecuteCommon` | ✅（本次修：费用累计 + 实例状态继承）|
| 01117 苦难给予者 | `DeathPipeline.ExecuteCommon` | ✅ |
| 03009 打工人 | `DeathPipeline.ExecuteCommon` | ✅ |
| 01511 心灵学者 | `DeathHandlers.Handle01511`（服务端直加；纯客户端走 `CmdReturnScholarToHand` → `TargetReceiveReturnedCard`）| ✅ 服务端 / AI；纯客户端本次补「整卡快照」（原先只有心灵学者 4 个字段）|
| 01313 嫉妒之辈 | `EnterHandlers.Handle01313` → `ReturnCardToOwner` | ✅ |
| 01322 残篇 | `HandManager.RemnantFinalize` → `ReturnCardToOwner` | ✅ |
| 01523 水墨 | `BoardSlot.InkEnterEffect` → `ReturnCardToOwner` | ✅ |
| 02205 战略性撤退 | `SpellHandlers.Handle02205`（在 `SimpleAI.PlaySpell` 的 `RunAsLocal` 里跑，`NetworkPlayer.Local` 此刻就是 AI）| ✅ |
| 02305 片甲不留 / 02306 屯能噩梦（挑获对方 1 张手牌）| `CounterManager.ResolveCardSteal` | ✅（本次修：原先人类走 `AddCardToHand(template)`、AI 走 `AddServerSideCard(template, iid)`，两边都只按模板新建 = 挑过来变新牌；现在按旧实例重建，且改成「先建后销」以免 `CopyFrom` 读到已销毁组件）|

**纯客户端的整卡快照**：`CardStateProto.FromCardInstance(...).SerializeCard()` 由 `CmdReturnScholarToHand` 的第 4 个参数带上，客户端在 `TargetReceiveReturnedCard` 里用 `NetworkPlayer.ApplyKeptCardState(inst, proto)` 套回 —— 只套「保留类」字段，再交给 `ApplyReturnToHandRules` 清临时状态。以后再有「远端重建手牌」的新路径，用同一对方法，别再手写字段表。

**未实现 / 无资产（不是回手 bug，别去查）**：03508 天选之人、02108 下班时间、03013 归息 —— 只有 `CardTextTable` 文案，既无卡资产也无 handler。
**死代码（无调用点）**：`NetworkPlayer.RouteReturnToHand`；`AddCardToHandForPlayer` 的 `oldInstanceID` 分支（且其内部传的是 `null`）。
**调试面板**：`UI/Panels/GetCardPanel.cs` 里 7 处 `AddCardToHandFromInstance` 都写死 `Local`，但该类无任何调用点（面板未接入流程）。
